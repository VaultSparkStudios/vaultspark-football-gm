# Task Board — Franchise Architect: Football

Public-safe roadmap. Session 8 audit + implementation sprint (2026-04-13). Session 9: test coverage (2026-04-13).

## Now

- [ ] Observe the first real opted-in cohort and verify freshness, suppression, deletion and abuse ceilings without manufacturing activity.
- [ ] Reconcile launch authority only from delivered/reply-as email evidence, SHA-bound founder launch approval, and the authoritative lifecycle registry.

## Session 86 — Tablet touch swipe for mobile nav drawer (CANON-041)

| Item | Status |
|------|--------|
| tablet-touch-nav-swipe — swipe right from left edge to open nav drawer; swipe left to close. Uses same `pointerdown`/`pointerup` + touch/pen pointer filter as sim-watch transport. `resolveNavSwipe` exported for direct unit testing. | ✅ Done |

**Verification:** `node --test test/nav-swipe-gesture.test.js` 9/9; shard-coverage guard 3/3; `node --check public/lib/appCore.js` clean.

**Carried from S62/S63:** this was the remaining CANON-041 touch-affordance item after the nav drawer shipped in S66. The "dedicated visual-evidence re-baseline" is a follow-up: the gesture logic is independently verified here; Playwright + responsive-evidence capture over the new swipe path is the natural next step for the founder to validate on device.

## Next

- [ ] Restore `/stats` to sitemap.xml the moment the community snapshot clears its suppression threshold. S94 withheld it so the first page indexed about this game is not a table of zeros; that is a temporary state with a defined exit condition, not a permanent decision.
- [ ] Re-run `/code-review` against a session's own diff BEFORE the canonical receipt, not after. S94 ran it late and it found ten real defects, which cost a full 45-minute shard re-run. The review is cheap relative to the receipt and should precede it.
- [ ] Reconsider the mobile tab-target count on its own terms. S94 skipped the 14-to-6 regroup because the rail is already grouped under five task headers and the remaining cost is target count on a phone — which is the founder-deferred item needing its own visual-evidence re-baseline budget, not a side effect of an information-architecture pass.
- [ ] Evaluate historical sparklines and shareable aggregate cards only after a real cohort proves they add value without weakening privacy.
- [ ] Offer aggregate-only Analytica ingestion through Studio Ark when that authority is ready; never export raw community receipts.
- [ ] Upgrade `actions/cache`, `actions/configure-pages`, and `actions/upload-artifact` when their official Node 24-native major versions are available; current CI is green under GitHub's forced Node 24 runtime, so this is advisory rather than a release blocker.

## Session 106 — Full arc: potential stops being drawn on a scale overall does not share (2026-09-12)

- [~] **MEASURED, IMPLEMENTED, MEASURED AGAIN AND REVERTED — the finding stands, the code does not.** The full measurement is below; what shipped is the comment recording it in `playerFactory.js`, not the change. It was reverted because `session90-development-environment` went red at 0.285 against a 0.25 tolerance and **stayed red (0.289) after the obvious follow-up fix — per-room trait centres — was implemented and measured too**. The cause is not a miscentred term: development deltas are applied to **ratings** and overall is recomputed with position weights, so changing *which* players move changes aggregate OVR even when the delta's mean is exactly zero (measured: reversion's mean contribution is 0.000). Taking gaps from mixed (37% negative) to uniformly positive is precisely such a redistribution. Closing that gate would have required duplicating the engine inside the test or widening its tolerance — and this project forbids allowlisting your own change through a gate. **Next session's item is the test, not the generator:** make its declared curve model the rating-level, position-weighted application it is compared against, keep the S90 subsidy (+0.84) as a negative control, and only then re-land position-relative potential.
- [~] **Make potential position-aware, and find out why nobody could close elite density.** `randomPotential` drew one distribution for every position (SUPERSTAR 84-98, HIDDEN 76-94, BUST 58-76, else 68-90) while `overall` is a position-**weighted** average of position-**biased** attributes. The two numbers lived on unrelated scales and only one knew what position the player plays. Measured at generation across four seeds and 8,832 rostered players: mean potential ~80 in **every** room against mean overall 73.6 (TE) to 81.7 (QB), **36.8% of the league generated above its own potential** (QB 51.8%, OL 49.0%, TE 22.7%), and all 45 players at 90+ in a fresh league were QBs (11) or offensive linemen (34) — **zero anywhere else**. Potential is now headroom above the player's own overall, sized by development trait, tapered to nothing by 31 and damped as he nears the ceiling. Generated above own potential: **36.8% → 0.0%**; mean potential now tracks the room (QB 85.8, OL 84.0, TE 77.2) while **every mean overall is unchanged to the decimal**, which is the evidence the RNG stream did not shift.
- [~] **The first version of that fix took two gates out of range, and measurement caught it before it shipped.** Position-aware potential alone read elite **5.3% out-of-range** and dispersion drift **0.157 out-of-range** over ten seasons, because the old draw's 37%-above-potential population had been a silent **downward counterweight** in the reversion term — removing it left only upward pull, and the rooms whose ceilings rose most (QB, OL) are exactly the ones that feed elite density. Damping headroom by proximity to the 99 ceiling restores the brake **without touching a threshold**: elite **1.8%** (from 2.8% at S105), dispersion drift **0.083** (from 0.095), parity **+0.034** (from +0.043). All three arms are better than the session inherited; elite and dispersion remain honestly `watch`.
- [x] **Give the ledger-budget gate its population.** It asserted four ledgers stayed under 96 KB while `logs/WORK_LOG.md` (213 KB) and `context/TASK_BOARD.md` (206 KB) — the two largest append-only files in the repo, and named as append-only in `ledger-roll`'s own header since S94 — were not in the list at all. Both are now registered with their own directory, order and heading patterns, and rolled: **572 KB → 244 KB**, S105 entries and the standing `Now`/`Next` sections intact. A new test derives the population **from disk**, so a ledger cannot be added without being policed.
- [x] Make the brief report the session authority that exists on disk rather than the one it read before self-healing it (S105's deferred finding).
- [x] Remove the doctor remedy that prescribed `check-last-session-summary.mjs`, a file this project's authority contract forbids by name and which `doctor.mjs` defines no check for — inert advice to violate a contract. A test now asserts no remedy names a forbidden path.

**Deferred with measurements, not silently:**

- [ ] **Elite density is `watch` at 1.8% against a sourced 1.53% ceiling — the closest this project has been** (S91 2.07%, S102 3.5%, S103 3.3%, S105 2.8%). The remaining gap is the position-blind 90+ cut itself: First-Team All-Pro is allocated **per position**, so a position-blind share compared with a per-position anchor is the same declared-population question S102-S104 settled for the parity gate. Take it as its own item with the question stated first; **do not adopt it as a way to turn the gate green.**
- [ ] Dispersion drift is `watch` at 0.083 against an on-target ceiling of 0.08 — 0.003 outside. Do not widen `stdDevDrift*`.
- [ ] **Ledger retention is positional, so "the last ten entries" is not "the ten newest sessions."** The live SIL still holds S78, S79 and S80 (in descending order, above the modern block) and WORK_LOG still holds S87, S95 and S96, because the roll keeps the last N entries *in file order* and those files have never been strictly ordered. Nothing is lost — the archives hold everything — but the working set is three entries short of what it advertises. The fix is retention by session **number**, which means reordering entries within a live file; content would still move verbatim, but it is a history rewrite and deserves its own session rather than a closeout. **S106 fixed the half that was this session's own fault:** the S106 entry was appended above S105 in four ledgers at once, and `test/session106-position-aware-potential.test.js` now fails when a newest-last ledger does not end with its highest-numbered entry.

## Session 105 — Full arc: every surface the brief reads gets a writer and a test that fails when it is skipped (2026-09-11)

- [x] Read the handoff as the session authority it was always meant to be. `parseHandoffCloseoutAuthority` knew only `# Session N Closeout` and `brief-semantic-fingerprint` carried a literal for `Where We Left Off (Session N)`; this project has written `# Latest Handoff — Session N → Session N+1` at every closeout since at least S96, and **none of twelve committed handoffs matched either pattern**, so the brief rendered `handoff=S?` for the project's whole recorded history and the divergence check silently ran on two sources. The shared parser now reads this project's shape (left-hand session only; the arrow's right side is intent), the fingerprint routes through it, and a test binds the *live* handoff to the newest SIL entry. The brief now renders `handoff=S104; divergent=false`.
- [x] Give the SIL rolling-status header a writer. It read `Last session: 100` through S104 and had never carried `Intent rate:`, so the brief rendered `Intent: ?%` beside five consecutive `Achieved` entries. `scripts/render-sil-rolling-status.mjs` derives the block from the live file and its archive; a studio test fails when the committed block lags a fresh derivation. **The derived figures differ from the hand-written ones and ship as derived:** Avg3 959.7 against 974.7, and 58 unique `/1000` sessions against a claimed 87 that the entries cannot reproduce. **Run `node scripts/render-sil-rolling-status.mjs --write` after appending every SIL entry.**
- [x] Make open premise decay across the whole audit history fail something. The corpus assertion walks every `docs/AUDIT_*.json` with a fixture negative control — and on its first run caught this session's own sidecar, whose shipped items were still marked `planned`.
- [x] Re-derive elite density under the roster shape S104 shipped, and decompose it. Canonical seed, ten seasons: active-roster 90+ share **0.4% → 2.8%**, still `watch` (S103 carried 3.3% on the pre-S104 shape). A probe reproducing the gate's clone path exactly (2.79% against the gated 2.8%) puts **64% of the elite cohort in the QB and OL rooms, which are 24% of the roster** — QB 11 of 95 (11.6%, the same rate S103 measured before S104 bounded the room), OL 19 of 311 (6.1%), everything else 17 of 1,279. Those are the only two rooms a generated league places **above their own mean potential** (OL 82.1 vs 79.8, QB 82.7 vs 80.9).
- [x] Close the `brief-preflight` importer question on the contract's answer (DECISIONS S105-4) instead of carrying it a fourth time.

**Deferred with measurements, not silently:**

- [ ] **Elite density: take the S105 room decomposition across two more seeds, then decide.** The candidates are (a) the OL/QB overall scale generated above its own potential — a generator defect with a measurable signature at season zero — and (b) a per-position elite reading against the per-position First-Team All-Pro allocation the anchor actually is. Do not adopt (b) to turn the gate green, and do not ship a cost without a matched control.
- [x] **Fix the context-ledger roll, which moved the wrong end and archived this session's own record.** `DECISIONS.md` hit the 96 KB ceiling and the gate's prescribed remedy (`ledger-roll.mjs --apply`) archived S105's decisions, truth-audit section and SIL entry, leaving the live SIL at sessions **85–98 with zero intent lines**. The roller assumed every ledger is newest-first — true of `CURRENT_STATE` alone, while three are append-only newest-last — and its entry patterns matched only the pre-S100 `— Session N` heading, so the cut landed at the eleventh *old-style* entry and every modern section beneath it moved as one block, reported as "2 entries archived" while moving six sessions. Reversed exactly from the committed archives (asserted, never reconstructed; `git checkout` deliberately not used on live ledgers carrying uncommitted work) and fixed at source: per-ledger `order`, suffix retention for newest-last, header kept live, archived block appended at the correct end, pointer kept at EOF, patterns matching both heading eras. Three tests, a negative control reproducing the inversion, and the invariant that every live ledger still holds this session's own entry. Re-rolled correctly: **286 KB → 147 KB, S105 intact.**
- [x] **Fix the intermittent browser test that blocked the production promotion, instead of re-running until it passed.** The promotion of `e541418` passed its gate and failed in `build` on `play-mode-smoke`'s weekly-evidence-trail assertion (`toBeVisible` → `hidden`), so `deploy` was skipped and production was never republished. The same test failed in S104's CI on `ccf68c2` and passed in S105's CI on the commit the promotion failed — intermittent, and not caused by S105, which touched nothing under `src/` or `public/`. It is the defect S103 fixed one spec over: a bye suppresses the tactic step (S102), the browser league is seeded randomly, and this spec asserted the tactic modal unconditionally. Now branches on what the product rendered, with the bye literals pinned against `describeWeeklyPlanReceipt` and a negative control. Local `npm run test:ui` 60/60. **The live-browser bye path remains unexercised locally and is recorded as such.**
- [x] Ship the permanently-stale PROJECT PROFILE row to studio-ops as Ark cargo (`01K291HHVU080E58C015E99770`); the renderer and the lens are both propagated, so no local change is legal.

## Session 104 — Full arc: the league was never generated at the roster its own rules declare (2026-09-10)

- [x] Generate a league at the structure the rules declare. `ROSTER_TEMPLATE` summed to **49** against a declared `activeLimit` of 53, and generation built **no practice squad at all** against a declared `practiceLimit` of 16. Every league in this project's history therefore began **20 players a club below its own steady state** and spent the next decade closing the gap. `PRACTICE_SQUAD_TEMPLATE` and a raised `ROSTER_TEMPLATE` now sum to exactly `activeLimit` and `practiceLimit`, and `test/session104-fieldable-depth.test.js` binds that arithmetic so neither constant can drift out of the other's reach — the failure mode `CONTRACT_RULES.maxSalary` shipped for years.
- [x] **That is the answer to the question S102 asked and S103 answered — and the question itself was wrong.** It was put as a choice between two populations. S103 showed `rostered` failed a prior test: a drift statistic requires a population that exists at both ends of its window, and a generated league's did not. What no session tested is that **`activeRosterOnly` fails the same test, mirrored**: an unfiltered 49 at the start of the window and the top 53 of 69 at the end, silently acquiring a selection filter. Neither candidate was a valid drift statistic and **choosing between them could not have produced one.** Measured on the canonical seed over ten seasons: rostered population **1568→2178 before, 2208→2183 after**; practice weight **0%→22.7% before, 23.2%→22.7% after**; between-group drift **−0.197 → +0.003**; within-group **+0.269 `watch` → +0.040 `on-target`**; `activeRosterOnly` **+0.303 `out-of-range` → +0.107 `on-target`**. The S103 gate now reports `development-dominated`, and its negative controls stay fixture-driven in `session102-gated-population.test.js` so the detector is still proved to fire against the pre-S104 shape.
- [x] Make the active 53 a **fieldable depth chart** rather than a leaderboard. `normalizeRosterSlots` labelled the top 53 by overall with no positional structure — and overall is not comparable across positions, so a pure ranking promotes quarterbacks and specialists and demotes the rooms that decide games. Measured over ten seasons: quarterback rooms **64 → 195** and specialists **64 → 155** while the offensive line fell 288 → 258 and the front seven 480 → 407, with **11.3% of quarterbacks at 90+**. `FIELDABLE_DEPTH` declares a `min` **and** a `max` per room and `assignFieldableActiveRoster` allocates in three passes — minimums, then merit bounded by each ceiling, then legality. **A minimum alone would not have fixed it:** nothing in a minimum stops a sixth quarterback out-rating a fourth cornerback for the slot, which is exactly how the room grew. The maximum is the term that binds; legality outranks both, so a structural preference can never shrink a roster.
- [x] Guard **both** release paths, and guard a **count** rather than a list of people. The roster-limit cut released the tail by overall and the cap cut released by value density — neither scalar is position-neutral, so an unprotected cap cut strips the line every time. **The first version of the guard protected the wrong thing and measuring it is what showed that:** protecting each club's best `min` players outright is a no-trade clause, and on the S91 camp-cuts fixture it left a club $129.0M over the cap bottomed out at the 53-man floor still **$27.8M over, reporting itself trapped** — while holding eleven linemen it was never allowed to touch. The guard is now a count evaluated as cuts accumulate; the release order is unchanged.
- [x] Make draft intake proportional to demand. `createDraftClass` sampled `rng.pick` over nine positions, so **every position received 11.1% of every class, 256 prospects a year, forever**, while demand ranges 1.9% (kicker) to 18.9% (line). Intake that does not match demand does not equilibrate — it accumulates on one side and starves the other, monotonically, and the ordering of the measured populations is exact: QB +230%, K/P +169%, OL +9%, front seven +16%. It also explains why the surplus concentrates in the *highest-rated* rooms. `DRAFT_POSITION_WEIGHTS` is derived from the declared template and drawn with the RNG's existing `weightedPick`, so exactly one value is drawn per prospect as before. Punters are drafted at last; excluding them while every club needs one is why the only route to a punter was a manufactured emergency-depth signing.
- [x] Keep the generated league **affordable**, which is the invariant this session broke first and loudest. Filling the declared roster on ordinary market contracts put **all 32 clubs over a $255M cap before a snap was played** — median space −$50.1M, worst −$84.7M — against a baseline of zero over-cap clubs and $43.4M median headroom. The compliance pass would have laundered it by cutting twenty players a club in the first offseason, so nothing would have gone red; the league would simply have started as fiction. **The fix is not to shave the salary curve until the number fits** — that is moving a threshold, and the curve is bound to `CONTRACT_RULES.maxSalary` by test. A real roster pays its starters and key backups and fills the rest at the minimum, and `FIELDABLE_DEPTH[pos].min` is already this project's declaration of who those players are. Asserted across four seeds, not the canonical one alone.
- [x] Stop the startup brief replaying **S100** as "last session", four sessions late. `brief-blocks.mjs` renders a `STALE LAST SESSION SUMMARY` block whenever `PROJECT_STATUS.lastSessionSummary` does not name the expected session, and `doctor-remedies.mjs` maps that check to `check-last-session-summary.mjs --fix` — **a script that did not exist**, and which `brief-preflight.mjs` imports *statically*, so the absence made the entire preflight module un-importable and nobody ever saw the `ERR_MODULE_NOT_FOUND` because nothing imports the preflight either. The remedy's own comment states the defect exactly: *"lastSessionSummary had a detector but no writer, so it silently went stale."* **The writer was written, and then removed — the studio shard was right and I was wrong.** `context/PROJECT_AUTHORITY_CONTRACT.json` names `scripts/check-last-session-summary.mjs` in `forbiddenFiles`, whose declared purpose is to *"fail closed when generic Studio propagation erases project-specific authority contracts"*, and this project declares exactly three permitted `statusMutators`. A generic writer would have been a fourth mutator of `PROJECT_STATUS.json`. It was deleted rather than allowlisted. **What actually closes the defect is this project's own authority: the summary is written at closeout, by hand**, and `currentSession` — which S103 left one behind — is reconciled so the brief's `currentSession - 1` arithmetic is satisfied by real values rather than by coincidence. The check that had been red for four sessions is green on true values.
- [x] Ship the **general** form as a gate that reads rather than executes — and get the invariant right, not the convenient one. The naive rule ("every static relative import under `scripts/` must resolve") would have demanded *creating a forbidden file*. The rule that is actually true: every static relative import resolves **unless** its target is contractually forbidden, in which case **the importing module must have no importer**, so the forbidden dependency can never be exercised. That second clause is load-bearing — wiring `brief-preflight` into anything now trips a test instead of failing at runtime. Comments are stripped before matching, because docstring usage examples name modules relative to the *caller*: 37 hits before stripping, one real one after.
- [x] Publish the player-facing release note the public status page owes. `check-public-truth`'s freshness gate counts sessions rather than days and tolerates exactly one — the note lands at closeout, so one behind is in flight and two behind is a page that stopped tracking the product. It was at **two**: S103 shipped without a note and the gate could not see it, because 103 − 102 sat exactly on the tolerance. `public/status.html` now carries a real note for what players will actually notice — clubs field a legal depth chart instead of six quarterbacks and four kickers, new leagues start at a full roster with a practice squad, the draft supplies the positions clubs need, and the cap binds from the first snap.
- [x] Write the premise checker the startup brief has been spawning into thin air. `scripts/lib/brief-preflight.mjs` spawns `scripts/check-audit-premises.mjs` inside a silent `catch`, and **the script was not in the repository**. A missing script and a clean run were the same observation from outside: `.cache/audit-premise-decay.json` had never been written, the SIGNALS row had never appeared, and **every typed `premises` block in `docs/AUDIT_*.json` had gone unchecked for the project's whole history** without anything saying so. The checker reuses `isAuditComplete` rather than declaring a fourth copy of the completion vocabulary, treats an unobservable premise as a third outcome so a renamed target can never read as "the defect is fixed", and is advisory by default with `--strict` for a caller that wants a gate. Run across the entire audit history: **zero unverified, zero open decay.**

**Deferred with measurements, not silently:**

- [ ] **The parity re-point is deliberately still not taken, and it is now a decision rather than an inheritance.** Both populations are valid drift statistics at last, so the choice is no longer forced — and `rostered` reads +0.038 to +0.063 across seeds against an `onTargetMaxAbs` of 0.15 where `activeRosterOnly` reads +0.102 to +0.115. Moving a declared population for no measured reason trades a comfortable margin for a tight one. The contrast stays published and ungated in `activeRosterMeanOverallDrift`.
- [ ] **The honest cost: the distribution gate's dispersion arm moved from `on-target` to `watch`** (+0.095/season on the canonical path, +0.105 and +0.111 on two probed seeds, against `stdDevDriftOnTargetMaxAbs` 0.08). This is not a regression this session caused — it is the same artefact seen from the other side. The old `on-target` was produced by the active roster's selection filter *strengthening* across the window, which compresses measured dispersion exactly as the underlying spread widens. With the filter constant, the arm reports the S91 random walk it was built to find. **Do not close it by widening `stdDevDrift*`.**
- [ ] Elite density: re-measure against the standing `watch` now that the position mix no longer drifts toward the highest-rated rooms. S103 named this as the measured candidate; the composition driver is removed, and the remaining reading is the intake/filter one, not the roster-shape one.

## Session 103 — Full arc: the denominator answered, the ratchet and the re-point both refused on measurement (2026-09-10)

- [x] Answer the parity target's denominator question, as its own item, with the question settled before any code changed. S102 published the divergence and refused to act on it; the answer is not a preference. **A drift statistic requires a population that exists at both ends of its window, and `rostered` does not** — a generated league is built at 53/club with an EMPTY practice squad and fills its 16/club practice slots over the following decade. Shift-share on the canonical seed 20260306 over ten seasons: **within-group +0.269/season** (`watch`) against **between-group −0.197/season** (practice weight 0% → 22.7%, at a mean ten points below the roster it joined), summing to the **+0.072/season this gate asserts as `on-target`**. Two effects of opposite sign cancelling inside a gate built to catch that — third instance here, after S91's free-agent pool in this same denominator and S102's dispersion arm.
- [x] Ship that finding as a machine-checked fact rather than a comment. `buildCompositionShift` decomposes the gate's own blended mean every run (midpoint weights and means, so the parts sum to the whole exactly) and the realism regression now asserts that it reports **`verdict-changed-by-composition`** on the canonical path. **This gate can no longer report `on-target` without also reporting, in the same receipt, that its denominator moved underneath it.**
- [x] Make the guard's criterion a changed verdict rather than a tuned magnitude. The first draft flagged composition when `|between| > |within|` and, run against the very measurement that motivated the item, returned `development-dominated` — 0.197 is not larger than 0.269, so it would have shipped green on the defect it was written for. It now classifies both drifts against the target's own bands and reports disagreement. No new free parameter was introduced; the bands already existed.
- [x] Give a league an identity. `staffSeedKey` and `coachingMarket`'s `leagueSeed` both fell back to `y${league.year}` when a league carried no `leagueId`/`franchiseId` — which is every single-player league, because only the multiplayer lobby path sets those fields. The year is not an identity, so `derivedRng` was working perfectly and being handed the same seed by every franchise. **Measured: byte-identical head coaches, coordinators and coaching-market candidate pools across seeds 8121 / 2026 / 4242.** `leagueIdentity` derives a per-league value from persisted content — the start year plus a canonical sorted sample of player ids, because `buildRandomizedTeamIdentities` takes only the year and a team-derived fingerprint would have reproduced the exact bug it fixes — and the normalizer assigns it once so it persists from the first load. No RNG draw, no snapshot schema change.
- [x] Verify, rather than inherit, the migration cost the handoff attached to that fix. The claim was that changing the seed key "regenerates staff for every existing save". It does not: `buildStaffProfile` preserves any stored name that is not the old role-label fallback and any finite stored attribute. Asserted on a real snapshot with its identity stripped to look pre-S103.
- [x] Check the third site rather than assume it. `gmDecisionAuthority`'s `normalizedFranchiseId` carries the same fallback shape and is **not** in this defect class: it keys an occurrence ledger held inside a single save's own state, so two franchises cannot collide through it. Left untouched, and said so, rather than "fixing" a non-defect for symmetry.
- [x] Re-justify the seed a fixture pinned, rather than relax the assertion, when the identity fix legitimately moved it. Derived staff and owner values feed `applyStaffToCoaching` into `team.coaching`, and coaching drives how a league develops, so every **freshly generated** league from a given seed now plays out differently — existing saves are untouched, because the profile builders preserve stored values. `rival-trade-offers`' S102 negative control pinned seed 620083 to surface its offer past the Week 11 deadline and it now surfaces at week 2. Its own note prescribes the response, so seeds were re-scanned against the live engine and **620097** adopted at week 12, with the cause written into the test. The condition is genuinely seed-sensitive: of twenty seeds scanned, three reproduced it and two produced no offer inside fourteen weeks at all.
- [x] Classify an intermittent browser red rather than re-run it, and fix the latent S102 test defect it exposed. `s63-surfaces` asserted the pre-game tactical brief becomes visible after one advance and handled a bye **inside** that modal — but S102 made a bye suppress the tactic step entirely (`tacticalPhase = regularSeason && !onByeWeek`), so on a bye there is no modal and that branch became unreachable. The browser league is generated from a random seed, so whether the controlled team plays in the first advanced week varies per run: **2 failures in 10 repeats** locally, with the captured artifact carrying the product's own receipt — *"Bye week committed · GM choice · bye week — no opponent · gm-decision → bye"*. CI passed on the two prior commits by luck. The test now waits for the brief **or** the bye receipt and asserts against whichever the product produced: **20/20** after the fix, with both bye-branch strings checked directly against `describeWeeklyPlanReceipt` output, because a run that never draws a bye would be a green that proves nothing.
- [x] Stop an empty league fingerprinting to a constant identity. `createLeagueBase` returns 32 teams and **zero players**, so a league normalized before its roster is allocated hashed an empty sample and produced one id for every league in existence — the exact defect `leagueIdentity` exists to fix, rebuilt inside its own fix and frozen into the save. `ensureLeagueIdentity` now refuses to persist below a fingerprintable floor; callers still get a deterministic derived key and the next normalize with a populated roster assigns the real one.
- [x] Recover S102's closeout, which had been deployed but never recorded. Its engine work was committed and both staging and production verified at `30572068`; the receipts, captures, rendered brief and status board sat uncommitted beneath a clean-looking history — the exact state a tree-only triage calls "not cut off". One commit, not a re-implementation. Its uncommitted `session102-lazy-frenzy-board` test was real work, passed 4/4, and was registered in the runtime shard.

**Deferred with measurements, not silently:**

- [ ] **The re-point itself was implemented, measured, and reverted — and this is the item that matters.** Pointing `LEAGUE_PROGRESSION_PARITY_TARGET` at `activeRosterOnly` reads **0.303/season** on the canonical seed, which is **`out-of-range`** against `watchMaxAbs` 0.3, not `watch`. A red gate is closed by fixing the defect it found or by weakening the gate; weakening is forbidden and the defect is real, so the population waits on the fix rather than being paid for with a threshold. **Do not close this by widening `onTargetMaxAbs`/`watchMaxAbs`, and do not close it by re-pointing the population a second time.** Everything needed to finish it is now on the receipt.
- [ ] **The measured root-cause candidate, from the same receipt: active-roster *position composition* is drifting hard.** `normalizeRosterSlots` ranks a club's whole roster by overall with no positional structure, so over ten seasons the quarterback room goes **64 → 195** players and specialists **64 → 155**, while the offensive line falls 288 → 258 and the front seven 480 → 407 — and the quarterback room carries **11.3% of its players at 90+**. High-rated, cheap positions crowd out linemen, inflating the active-roster mean for a compositional reason one level below the practice-squad one. This is a strong candidate for both the 0.303 parity reading and the standing elite-density `watch`, and it is the thing to fix before the re-point.
- [ ] **The survivorship ratchet's recommended cost was implemented, measured against a matched control, and reverted.** S102's candidate was an age-indexed decline in potential. Built it (`onsetAge 27`, 0.35 points per year past prime, capped at 3.0, RNG-free, applied at the reversion seam) and ran it against a matched control on seed 2026 for ten seasons, changing nothing else:

  Control against decline, at simulated season 10: active-roster mean drift **+0.265/season against +0.298**, elite 90+ share **3.80% against 3.92%**, active-roster mean potential **83.78 against 82.91**, active-roster dispersion **4.837 against 4.830**.

  The cost is real — mean potential falls 0.87 — and it lands on the wrong thing. Elite density does not move and the parity reading gets worse. The mechanism appears self-defeating: declining veterans wash out faster, accelerating the youth turnover that supplies the high-potential intake driving the ratchet. Measured age mix at season 8: the active roster's mean age has fallen 27.38 → 26.0, the elite cohort's mean age is 26.6, and only 34% of it is past the decline onset at all. Reverted; no dead module left behind.
- [ ] Elite density remains `watch` at 3.3% of the active roster at 90+ against a sourced 1.53% All-Pro ceiling, with the diagnosis re-derived rather than re-inherited: the driver is not veterans holding ceilings but intake supplying a fixed potential distribution into a filter that grows more selective every season — and, on this receipt, into an active roster whose position mix is itself drifting toward the highest-rated rooms.

## Session 102 — Full arc: the ledger, the pool, and the population a gate declares (2026-09-09)

- [x] Persist the term a signing bonus is amortized over, so a deal's cap hit is fixed when it is signed instead of climbing every year it survives. `normalizeContract` computed the term into a local and threw it away, so every reader re-derived proration from whatever years were left: measured +23.0% over 3 years, +26.6% over 4, +28.9% over 5, with the final year always costing the full salary. Legacy saves migrate to their remaining term plus void years — the exact value the discarded local held — and stop accelerating from the load forward. The field is additive and self-migrating, so no snapshot schema break; both directions of the round trip are asserted.
- [x] Make releasing a player accelerate every remaining year of proration rather than one, so a bad contract is actually hard to escape and a June 1 designation splits real multi-year money instead of splitting a single year 55/45. The total is rounded once and then split, so deferring money can neither create nor destroy a dollar of it.
- [x] Persist a restructure's re-prorated schedule, so the relief it buys holds for the rest of the deal instead of tightening again every season.
- [x] Charge the fifth-year option against the cap. `capHit` arrived through an object spread and was never recomputed, so exercising the option raised salary and base salary while the club paid nothing at all.
- [x] Give the free-agent pool an exit. Intake ran every offseason against a retirement roll of ~0.03% for an unsigned 24-year-old, so the pool only grew. A player nobody signs for three consecutive offseasons is out of the league, applied as a deterministic rule that still consumes the retirement roll — short-circuiting it would shift the RNG stream and silently re-calibrate every seeded league. Measured on seed 2026 at simulated season 10, same code, rule disabled through its declared window: **400 unsigned players without the rule, 296 with it**, and the bounded series flattening (293 → 296) where the control accelerates (346 → 400).
- [x] Make both arms of the distribution gate read the population the gate declares. `LEAGUE_DISTRIBUTION_TARGET` has read "active-roster overall dispersion and elite density" since S92, and S92 moved the elite arm onto `activeRosterOnly` because All-Pro honors are drawn from the active roster and the practice squad is structurally ineligible — but the dispersion arm beside it kept reading the blended `stdDevOverall`. A receipt describing itself as two readings of one league was two readings of two different leagues, and they disagreed about the verdict: blended sd 4.318 → 6.048 (drift 0.173/season, **out-of-range**) against active-roster sd 4.318 → 4.690 (drift 0.037/season, **on-target**). The blended reading is still published beside it, ungated, as `distribution.blendedRostered`.
- [x] Assert the dispersion arm in `realism-career-regression` for the first time. S101 recorded that this regression checked only the arms that passed; the reason the third arm could not be asserted was not the generator but that it was measuring a population no target declared.
- [x] Measure the development curve's trait centre from the live league instead of declaring it. `(potential - LEAGUE_AVERAGE_POTENTIAL)` is the third literal centre this function has carried: at 70 against a league at 79.9 it handed every player a standing +0.50 an offseason, S91 replaced the literal 70 with the literal 80, and the active roster's mean potential then climbs to 83.57 by season 10 — so the differentiator becomes a subsidy again as the league inflates. The reversion term beside it has been measured from the live league since S91 for exactly this reason; the trait term now is too, over the same population, for the same conservation argument.
- [x] Fix the derived RNG's range reduction. `int` and `pick` reduced with `hash % n`, which keeps only FNV-1a's weakest bits and degenerates to a bit mask for any power-of-two pool, so sibling seed keys collided hard. High-bit reduction plus a murmur3 `fmix32` finalizer: **13/32 → 31/32** distinct pairs across 32 sibling team keys, where independence predicts about 30. This improves every derived value in the game, not only names.
- [x] Give every coach a name. `buildStaffProfile` fell back to the role label, so every head coach in every league was "Head Coach" and `nodeForStaff`'s (teamId, role, name) identity carried no information in its name column. Names derive from a keyed source and never draw from the session stream, so adding them shifts no caller's RNG position. Measured 31/32 distinct head coaches on three seeds.
- [x] Give the bye week its own beat. Every regular-season week demanded a tactic, including the one week the team does not play; the film receipt came back null because there was no film, and no beat fired — the bye was the only week that asked for a decision and returned nothing for it. Whether the controlled team plays is now engine-owned truth on the dashboard rather than inferred from a client cache that is cold on exactly that path.
- [x] Bring the initial shell back under its 650 KB target by making the trade-deadline frenzy board a declared lazy root. Its call site was already guarded to the declared deadline window; only its download was not. **642,587/650,000 bytes, 52/58 modules, zero lazy leaks** — 7,413 bytes of headroom against the 848 this session inherited. The budget was not raised.

**Verification:** measured through `runRealismVerification({ seasons: 10 })` on seed 2026 — the same path `realism-career-regression` asserts on. Distribution: dispersion **on-target** (0.037), elite **watch** (3.5% against the sourced 1.53% All-Pro ceiling and 5.19% Pro Bowl watch line), rooms 7/7 on-target. Parity: rostered mean drift 0.055, on-target. Every new or repaired gate carries a negative control that reproduces the pre-fix arithmetic explicitly rather than restoring the defect in the source.

**Deferred with measurements, not silently:**

- [ ] The parity target's own denominator is load-bearing, and the divergence is now published rather than acted on: the same mean drift reads **+0.055/season** on the declared `rostered` population and **+0.282/season** on `activeRosterOnly`, which would classify as `watch`. `LEAGUE_PROGRESSION_PARITY_TARGET` declares `rostered` and measures `rostered`, so this is not an implementation defect — re-pointing a declared target at a population that turns it red is a calibration decision, and one that would be indistinguishable from the outside from moving a threshold until the number fit. It is emitted as `progression.activeRosterMeanOverallDrift` (ungated, with `wouldClassifyAs`) so the decision can be taken deliberately with the numbers in hand. Take it as its own item, with the question stated first: is the league the gate is meant to police the one the GM competes in, or the one the roster rules define?
- [ ] Elite density remains `watch` at 3.5% of the active roster at 90+, against a sourced All-Pro ceiling of 1.53%. The centre fix moved it (3.60% → 3.20% on the ad-hoc 10-season probe) but did not close it, because the residual driver is survivorship rather than a stale centre: mean potential on the active roster climbs 79.92 → 83.57 over ten seasons as low-potential players wash out while intake keeps supplying the same distribution. A ratchet needs a recurring cost; the candidate is an age-indexed decline in potential, which is both the realistic model and the one that prices the ratchet. Not attempted here — it changes career arcs and needs its own 10-season re-measurement across seeds against the career-length bands.
- [ ] Every league generated in the same start year shares one set of coaching-staff names. `staffSeedKey` falls back to `y${year}` when a league carries no `leagueId`/`franchiseId`, so the derived identity is not per-franchise. Measured: byte-identical head-coach names across seeds 8121, 2026 and 4242. Fixing it means changing the seed key, which regenerates staff for every existing save, so it wants a migration decision rather than an edit.

## Session 101 — Full arc: the advertised rules become enforced rules (2026-09-09)

- [x] Declare the trade deadline once in league settings and enforce it at the shared trade command seam, replacing three surfaces that advertised three different windows for a rule the engine never checked.
- [x] Stop `negotiateAndSign` hand-writing a contract with the old cap hit when the cap authority refuses, and report the terms actually signed instead of a flat "accepted the offer".
- [x] Rebuild the whole declared `team.season` shape on the restore path (third drift site) and normalize the league before `new StatBook` so an omitted collection cannot throw after the compatibility check passed.
- [x] Route rewind snapshots through the save codec, refuse a restore whose recovery point could not be written, and reconcile a corrupt rewind index against the payloads actually present.
- [x] Compute cap rollover against the grown cap, guard `processStaffLifecycle` against the repeated pipeline call, and expire all seven staff roles rather than three.
- [x] Make the deploy gates run the behaviour shards that cover `src/**`, with a structural test that fails on the pre-fix workflow.
- [x] Replace the tautological lifecycle-drift assertion with fixture-driven negative controls, and report an unresolved authoritative registry as unresolved instead of omitting the check.
- [x] Let `git-head-covered-by-publication` and an anonymous-but-reachable origin contradict a verified release claim; make an unverified live origin an explicit freshness state.
- [x] Assert `fromSnapshot` TradeService parity against the live session rather than absence-of-throw.
- [x] Report navigation honestly when the target is in another tab or hidden, and repoint the two call sites that pointed into the wrong tab.
- [x] Populate the two team selects that shipped empty, remove the one the data cannot support, wire the commissioner intent queue to its existing endpoint, and token-ize seven hardcoded reds with no light-theme path.
- [x] Ship an Ark `registry-delta` correcting the authoritative registry to FORGE, rather than flipping the local contract (which would vacuate three blocking lifecycle gates).

**Verification:** canonical Node **1,369/1,369** across six shards (core 210, runtime 784, sim-contract 83, sim-realism 1, long 5, studio 286). The studio shard was rerun after two self-caused reds on its first pass — a stale `static/` build whose modulepreload list predated this session's new module (the earlier build had been blocked by the public-truth gate), and a Command Center fixture that predated the declared `tradeDeadlineWeek` setting. Both were fixed at source; neither was flaky, sibling drift, or force-greened. The rerun also covers the writeback-currency test, which was registered after the runner had already loaded its shard map. Doctor `blockingFailing 0` with the one standing lifecycle-registry warning. Pages build, browser module reachability, promise observability, boot budget (649,152/650,000 static bytes, 53/58 modules, zero lazy leaks) and public-truth (42 engine systems · 102 shipped surfaces) all green.

**Deferred with measurements, not silently:**

- [x] RESOLVED IN S102 — Signing-bonus proration is recomputed over *remaining* years (`src/domain/contracts.js:120`), so a contract gets more expensive every year it exists — measured +18.3% over 3 years, +26.4% over 4, +22.8% over 5, and a deal's final year is its most expensive, which is backwards. `normalizeContract` already computes a `capYears` local that is never persisted. The fix is a save-shape change (persist the original term, migrate existing snapshots by defaulting to current `yearsRemaining`) and was too large to land safely alongside this session's other persistence work.
- [x] RESOLVED IN S102 — The free-agent pool is unbounded (0 → ~413 players by simulated season 10, ~60/season with no exit path) and sits inside the reversion gap centre that `progressionParity.js` explicitly fences it out of. Measured displacement of the centre every rostered player converges toward: 0.00 at season 0, 0.44-0.51 by season 10. Bound the pool — force retirement after N consecutive unsigned offseasons — rather than re-scoping the centre; the conservation argument in `potentialReversion.js:117-134` is correct.
- [~] PARTLY RESOLVED IN S102 — the arm was reading a population no target declared; corrected to ctiveRosterOnly, where dispersion is on-target (0.037) and the blended reading is published for contrast. The elite-density half of this item stands and is re-deferred with fresh numbers in the S102 section. Original entry: the engine's own `buildDistributionReceipt` reads `dispersionStatus: out-of-range` on all three probed seeds (annual sd drift 0.165-0.171 against a 0.15 ceiling; 90+ share 0.3-0.45% → 2.97-3.51% on a fixed active-roster denominator), while `realism-career-regression.test.js` and `session92-nfl-elite-density-baseline.test.js` assert only the *elite* and *global mean* arms — the arms that pass. The mean is on-target (77.28 → 77.67), so this is a shape defect a mean gate cannot see. Wiring the dispersion assertion turns the suite red immediately, which is the point; the root is that the generator's implied 90+ share is ~3x the sourced All-Pro ceiling, so the fix is `playerFactory.js`'s SUPERSTAR band, not the reversion rate. Needs its own session with a 10-season re-measurement budget.
- [x] RESOLVED IN S102 — Every coach in a generated league shares one name ("Head Coach", "Offensive Coordinator") — `buildStaffProfile` falls back to the role label, and coordinators never get real names. Immersion, and it weakens `nodeForStaff`'s (teamId, role, name) identity.
- [x] RESOLVED IN S102 — A bye week still demands a tactical decision and returns no feedback: `weeklyPlanComposer.js` forces the tactic step for every regular-season week, the film receipt is null, and no beat card fires. The client already reads `schedule.byeTeams`; pass it in and give the bye its own beat.

## Session 99 — Tie-honest payoff and first-debrief evidence (2026-09-08)

- [x] Replace two-state post-game result copy with one win/loss/tie authority across recap, Franchise Moment, press-room questions, effects, promises, receipts, and continuity.
- [x] Add exact 20–20 regressions that prevent every named emotional surface from turning a tie into a loss.
- [x] Offer a one-time, per-franchise private pulse after the first successful weekly debrief: four explicit ratings, optional note, no automatic transmission, and no save payload or player identity.
- [x] Make dismissal and completion focus-safe, copy feedback truthful when the Clipboard API is unavailable, and never prompt again once answered or declined.
- [x] Inspect and hash-bind the new surface on desktop/mobile in dark/light themes; correct the light-theme contrast defect found in the first render.

**Verification:** canonical aggregate **1,319/1,319** across six shards; Playwright **55/55**; focused release guards **44/44**; Pages build, browser-module reachability, static smoke, and CANON-053 visual receipt green. Lazy-loading the post-game reward layer holds the first-decision graph at **643,495/650,000 bytes**, 52/58 modules, with zero lazy-root leaks. Application and release evidence are committed and pushed; exact staging, production Pages and backend deployment are verified. Public launch remains **HOLD** pending delivered reply-capable project-domain email evidence, exact-SHA founder approval, and authoritative lifecycle reconciliation.

## Session 98 — Full arc: the first season pays off, chronology stays true, and tablet focus comes home (2026-09-03)

- [x] Replace the never-initialized dashboard-phase sentinel with one pure previous/current season-transition authority.
- [x] Prove initial load and ordinary refresh remain quiet while the first regular-season or postseason completion opens the existing season review exactly once.
- [x] Preserve `GameSession.getRecentBoxScores()` year/week-descending authority in Trophy Road instead of applying a destructive week-only re-sort.
- [x] Consolidate duplicate Arrow/Home/End handlers into one declared-orientation keyboard authority.
- [x] Preserve desktop roving focus and return tablet focus to the visible drawer toggle after selection closes and inerts the drawer.
- [x] Bring the corrected boot graph under the strict 650 KB initial-shell budget without declaring a lazy island as a preload.
- [x] Bind Node, browser, responsive-pixel, hosted-performance, staging, production Pages and backend deployment evidence to source `3cce0a51e82e78625bdfad82f1cbdb19a31c94aa` and artifact `6bdc133a478cfee8a8b19321a5684001b65e3322210e1a090b573785240bc7f4`.

**Verification:** canonical Node 1,303/1,303 across six shards; exact-SHA CI workflow 33769176381 green; Playwright 55/55 locally and in the release workflow; boot 649,716/730,000 bytes with 53/58 initial modules and zero lazy leaks; 255 responsive game states with 98 retained exact-candidate captures plus four reviewed status-page captures; stable staging 14/14 at deployment `a835d039-bd18-4084-8eec-5a60b81993aa` with rollback; hosted `/` and `/game.html` performance verified; production Pages workflow 33770515766 and backend workflow 33770517153 use the exact candidate. Technical deployment is complete; public launch remains HOLD.
## Session 97 — Full arc: tie-complete franchise truth and a reachable phone commit (2026-09-02)

- [x] Establish one browser record authority and one engine helper for W-L-T formatting, standings lookup, half-win percentages, and exact deltas.
- [x] Migrate every reproduced record consumer across mobile, newsletters, rewards, season epilogue, achievements, return digest, server/local fan sentiment, adaptive difficulty, beat reporting, General Manager authority, and narrative events.
- [x] Prove a tie-only return delta is visible and a 5-0-1 season is neither rendered nor scored as 5-0/perfect.
- [x] Isolate doctor health-receipt freshness from substantive `lastUpdated` activity and pin the real command path.
- [x] Make the phone decision overlay a named, four-edge-safe region with a sticky theme-correct action dock.
- [x] Bind tests, inspected pixels, hosted performance, stable staging, production, and release authority to exact source `95597906033ced89469cd2c268a661d5a3b2b4a8` and artifact `47b3cee3288715e8560cb877a1337c13366f39e2a46335e7ef2be7e2487e2b89`.

**Verification:** canonical Node 1,300/1,300; exact-SHA CI 1,295/1,295 across five shards in workflow 33595882214; Playwright 54/54 locally and green in the workflow browser gate; 255 responsive states with 98 retained reviewed dark/light desktop/tablet/mobile captures; Pages build, module reachability, boot budget and smoke green; sanitization critical/warning 0/0; sitemap 9/10; staging 14/14; production 10/10; hosted performance verified; promotion workflow 33598681683 green. Technical deployment is complete. Public launch remains HOLD.

## Session 96 — Full arc: trustworthy orientation, diagnosable release identity, and mobile parity (2026-08-26)

- [x] Make startup orientation reject invalid dates, parse inline SIL v3 categories, derive intent from entry bodies, and content-refresh stale Genius authority.
- [x] Add a deterministic per-file artifact ledger and a reusable promotion-bind verifier that prints both identities plus first-file deltas.
- [x] Turn Cloudflare aggregate analytics reporting into a repeatable secrets-gateway-backed receipt while withholding user, engagement, retention, and cohort claims.
- [x] Raise the mobile trigger to 44×44px and make the 100dvh drawer four-edge safe-area aware.
- [x] Root-fix the cold lazy-island Escape error found by rendered pixels.
- [x] Replace wall-clock animation waiting with bounded rendered-geometry settlement after Linux CI exposed the portability defect.
- [x] Bind visual, hosted-performance, stable-staging, and production authority to `93b867e010dfe3773116433e5b8068dc894e7c0c` / `c45c4a49a46da9e0438219744b8e42e30fb96125ba8c414881c665fe766783a6`.

**Verification:** implementation-boundary canonical Node 1,293/1,293; final-SHA CI 1,290/1,290 across five push shards in workflow 33030325197; Playwright 54/54; focused release/staging checks 26/26 and 19/19; final workflow responsive evidence 255 states with 240/240 required and 96 retained/reviewed; staging 14/14; production 10/10; promotion workflow 33031371311 green. Technical deployment is complete. Public launch remains HOLD.

## Session 95 — Full arc: one decision beat per playoff round, one exact artifact through production (2026-08-26)

- [x] Gate Pages publication on the full browser suite and repair the stale development-profile assertion.
- [x] Persist postseason round state so one command resolves one controlled-team game; preserve exact plans across save/resume and make whole-season delegation consume every gate.
- [x] Centralize phase-to-surface navigation and prove exact destinations exist and receive focus.
- [x] Calibrate owner capital as football-operations liquidity with obligations, runway, trait-weighted appetite, bounded distributions, and deterministic 1/8/15/40-year equilibrium gates.
- [x] Replace the Gist forgery claim with an honest checksum boundary and optional versioned PBKDF2/HMAC authentication with legacy-save support.
- [x] Align realism workflow timeouts, persist progress, and upload partial evidence under `always()`.
- [x] Bind rendered pixels, hosted performance, stable staging, and production to SHA `92e4190a4221383c8352ea6321d65528cb449e74` and artifact `ca66c9210c1efbd234c31d9f392ca70dd6aa03171b47c90057b32d02210767e7`.
- [x] Remove repeated Hall of Fame candidate rescoring; the measured two-season dashboard probe improved from about 16.8 seconds to 7.1 seconds.
- [x] Root-fix the release-only digest mismatch: Pages now checks out full Git history so git-derived sitemap lastmod values are deterministic across local, staging, and CI builds.

**Verification:** canonical Node 1,280/1,280; Playwright 54/54; responsive 251 states with 92 retained/reviewed plus 236 final-CI required states; staging 14/14 with rollback; production 10/10; workflow 32933281222 green. Technical deployment is complete. Public launch remains HOLD.

<!-- ledger-roll:pointer -->
---

Older entries are retained verbatim in `context/archive/TASK_BOARD.archive.md`. Nothing is summarised or removed on the way; the live file holds the working set only (newest 10 entries), so a reader does not pay for the whole project's history to learn what is true this week.
