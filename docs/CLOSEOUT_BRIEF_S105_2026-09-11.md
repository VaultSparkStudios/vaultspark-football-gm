# Closeout Brief — franchise-architect — S105

> S104 found one brief row reading a surface nothing writes. S105 read every derived row of the brief and found three more — including a session authority that had never been read in the project's recorded history.

## Shipped

- **The handoff becomes the session authority it was always meant to be** (7/10 project, 8/10 ecosystem): resolveSessionAuthority takes the monotonic maximum of SIL, status and handoff and reports divergence when they disagree. Its handoff parser knew only '# Session N Closeout', and brief-semantic-fingerprint carried its own literal for 'Where We Left Off (Session N)'. This project has headed every handoff '# Latest Handoff — Session N → Session N+1' since at least S96, and none of twelve committed handoffs matched either pattern — so the brief rendered handoff=S? for the project's whole recorded history, divergent=false was a two-way comparison presented as a three-way one, and a handoff lagging SIL could never have been detected. The shared parser now reads this project's shape (left-hand session only; the right side is intent), the fingerprint routes through the same parser so the heading is declared once, and a test binds the LIVE handoff to the newest SIL entry with a negative control reproducing the pre-S105 pattern. The brief now renders handoff=S104; divergent=false. Shipped upstream as Ark cargo so propagation does not revert it.
- **The SIL rolling-status header gets a writer, and its numbers turn out not to be reproducible** (6/10 project, 7/10 ecosystem): The brief reads Last session, Intent rate and the averages from the rolling-status block at the top of the SIL, and nothing wrote it: it read 'Last session: 100' through S104 and had never carried an intent line, so the brief showed 'Intent: ?%' beside five consecutive Achieved entries. scripts/render-sil-rolling-status.mjs derives the block from the live file and its archive, and a studio test fails when the committed block lags a fresh derivation. The derived figures differ from the hand-written ones and ship as derived: Avg3 959.7 against 974.7, and 58 unique sessions scored /1000 against a claimed 87 that the entries cannot reproduce. The block names the count it read. No normalisation rule for older score scales was invented to reconcile the two.
- **Open premise decay across the whole audit history now fails the studio shard — and caught its author in the first minute** (5/10 project, 8/10 ecosystem): check-audit-premises.mjs reads only the newest sidecar and is advisory, and its one caller cannot be imported here by contract, so S104's 'zero open decay across all 83 sidecars' was a single manual reading. The new assertion walks every docs/AUDIT_*.json through checkAudit with a fixture negative control. On its first run it went red on a live instance: this session's own sidecar, whose first three items had shipped while still marked planned. Resolved by recording the evidence, not by exempting the current audit.
- **Elite density re-derived under the roster shape S104 shipped** (6/10 project, 3/10 ecosystem): S104 asked for this before anyone proposed a cost. Canonical seed 20260306, ten seasons, the runRealismVerification clone path: the active roster 90+ share goes 0.4 to 2.8 per cent, down from the 3.3 per cent S103 carried on the pre-S104 shape, still watch against the sourced 1.53 per cent All-Pro ceiling. Dispersion (0.095/season) and parity (+0.043) reproduce S104 to the third decimal. A probe that reproduces the clone path exactly - its 2.79 per cent equals the gated 2.8 - decomposes the cohort: QB 11 of 95 (11.6 per cent), offensive line 19 of 311 (6.1), every other room 17 of 1,279. QB and OL are 24 per cent of the roster and 64 per cent of its elite. S104 fixed how many quarterbacks a club carries, not how highly a quarterback rates: the QB rate is the same 11.3 per cent S103 measured before the room was bounded. The measured candidate sits at generation - OL and QB are the only rooms generated above their own mean potential (82.1 vs 79.8, 82.7 vs 80.9).
- **A three-session open question closed on the contract's answer** (3/10 project, 3/10 ecosystem): The S104 handoff asked, for the third time, whether render-startup-brief should import brief-preflight. It cannot: brief-preflight statically imports a file PROJECT_AUTHORITY_CONTRACT.json forbids, and S104's import gate fails the suite the moment it is wired. Recorded as DECISIONS S105-4 rather than carried a fourth time.
- **The permanently-stale PROJECT PROFILE row shipped to its owner** (2/10 project, 6/10 ecosystem): The row reads a 30-minute TTL cache written only by studio-ops' project-profile.mjs, which this repo does not carry; the brief renders at closeout, so the row can only ever say 'refresh required'. Renderer and lens are both propagated, so no local change is legal. Shipped as Ark pattern-share cargo 01K291HHVU080E58C015E99770 with the proposed fix.
- **The context-ledger roll moved the wrong end, and this session caught it by being archived by it** (8/10 project, 7/10 ecosystem): DECISIONS.md hit the 96 KB ledger ceiling, and the gate prescribed remedy - ledger-roll.mjs --apply - archived this session own decisions, truth-audit section and SIL entry, leaving the live SIL holding sessions 85-98 with zero intent lines. The roller declared ledgers are newest-first here, true of CURRENT_STATE alone while three others are append-only newest-last, so it kept the oldest ten and archived everything newer. Independently its entry patterns matched only the pre-S100 heading form, so for DECISIONS the cut landed at the eleventh old-style entry and every modern section beneath it moved as one contiguous block - reported as 2 entries archived while moving six sessions. Reversed exactly (current archive minus committed archive, asserted before any write; git checkout deliberately not used on the live ledgers, which would have discarded this session uncommitted work). Fixed at source: per-ledger order, suffix retention for newest-last, file header kept live, archived block appended at the correct end, pointer kept at EOF so the next roll cannot delete everything beneath it, and patterns matching both heading eras. Three tests plus a negative control reproducing the inversion, and a direct invariant that every live ledger still holds this session own entry after a roll. Re-rolled correctly: oldest 38/12/12 archived, 286 KB to 147 KB, S105 intact.

## Follow-ups

- **Run render-sil-rolling-status --write after every SIL entry**: The studio shard fails when the committed header lags the entries. That is the point: the header is a projection, and a closeout that appends an entry and forgets the header is exactly the drift that left it at S100 for four sessions.
- **Elite density: no cost without a measured mechanism**: Take the S105 room decomposition across two more seeds, then decide between the OL/QB overall scale being generated above its own potential (a generator defect with a measurable signature) and a per-position elite reading against a per-position All-Pro anchor. Do not adopt the second as a way to turn the gate green; S102 age-indexed decline was already refused on a matched control in S103.
- **doctor-remedies maps last-session-summary to a forbidden writer**: The remedy names check-last-session-summary.mjs --fix, which PROJECT_AUTHORITY_CONTRACT.json forbids here. Not changed: doctor-remedies is propagated. Raise with studio-ops alongside the S105 cargo if it matters.
- **render-startup-brief prints session authority from pre-heal status**: The first S105 closeout render healed currentSession 104 to 105 and, in the same run, printed status=S104 divergent=true; a second render printed divergent=false. The value on disk was right both times. Recompute resolveSessionAuthority after the heal. Only visible because the handoff is now a third authority.

## Blockers

- **Public launch remains HOLD — unchanged and untouched this session**: Delivered reply-capable project-domain email, candidate-bound founder launch approval, and authoritative lifecycle reconciliation remain three independent unmet gates. launchReady stays false. This session is a technical deployment only.
- **Authoritative registry still reads SPARKED against a local contract of FORGE**: Non-blocking doctor warning, unchanged since S101. Do not resolve by flipping the local contract: three blocking lifecycle checks are written to pass when the expected value is not FORGE.

## Honesty Ledger

- **The new corpus gate's first red was this session's own work**: Items 1-3 shipped while the sidecar still marked them planned, and the gate written in item 3 went red on that sidecar within a minute of existing. That is recorded as the gate's strongest negative control, not smoothed over.
- **Two of my own first drafts were wrong and a test caught both**: The rolling-status intent rate first divided by all five recent entries, counting an entry that recorded no outcome as a miss; the fixture test caught it and the code was fixed, not the fixture. The first sparkline rescaled min-to-max within five scores, which renders a one-point move as a cliff; it now uses a fixed 900-1000 scale.
- **The derived SIL averages are lower than the ones previously published**: Avg3 derives to 959.7 where the brief printed 974.7 for four sessions. The published figure was never derived from the ledger. The lower number ships, and PROJECT_STATUS.silRollingAverages is written from the same derivation.
- **Nothing a player sees changed**: Every S105 change is to observability, record-keeping and measurement. No player-facing release note is owed; the status page's freshness gate tolerates exactly one session.
- **Measurement scope declared rather than implied**: Elite, dispersion and parity figures are one seed (20260306) over ten seasons via the runRealismVerification clone path. No cross-seed reading was taken. SIL scores are engineering assessments, not measured player outcomes; no real-cohort evidence exists and none was manufactured.
- **This session's lock was deleted by something outside this repo, and the cause is recorded as unexplained**: context/.session-lock was written at /start and found missing about forty minutes later, just after the canonical suite launched. No script or test in this repo deletes it. The only studio-ops code that unlinks a lock is closeout-autopilot.mjs, which reaches this repo only when run with --project pointing here; a second Claude session was live on the machine and its arguments could not be recovered. Not attributed without evidence. The lock was rewritten, and the rewrite reads session_id 106 because the writer derives the id from the already-closed S105 record - transient, gitignored, cleared at closeout.
- **The session obeyed a gate remedy that corrupted its own record, and the reversal was asserted rather than assumed**: Running the ledger ceiling prescribed fix archived S105 record. The reversal computed the moved block as the current archive minus its committed body and refused to write if the current body did not end with the committed one, so nothing was reconstructed by hand. git checkout was not used on the live ledgers because those carried this session uncommitted work. The recommending gate would have gone green on the corrupted result - smaller files, pointer present, archive non-empty - which is why the fix ships with an invariant asserting the live ledger still holds the session own entry.

## Proof

- Files changed: 22
- Insertions: 1991
- Deletions: 1127
- Suite: canonical Node 1,457/1,457 across six shards (core 245, runtime 817, sim-contract 83, sim-realism 1, long 5, studio 306), up from 1,446/1,446 in S104. The full npm test run read 1,450/1,452 with every behaviour shard green on its first pass and two self-caused studio reds - the new SIL rolling-status gate red on byte-identical CRLF-vs-LF text, and the 96 KB context-ledger ceiling whose prescribed remedy then archived this session own record. Both fixed at source and the studio shard re-run green at 306/306; neither was flaky, sibling drift, or force-greened. Nothing under src/ or public/ changed after the full run. Other gates green: audit premises 6 verified and 0 open decay across the corpus, brief format conformant, session-authority binding the live handoff to the newest SIL entry, SIL rolling-status current.
