# /initiate Prompt

This public repo is already initiated. New-project onboarding is owned by the canonical Studio OS protocol in `docs/SESSION_PROTOCOL.md` section 4.

If `/initiate` is invoked here, follow `docs/SESSION_PROTOCOL.md` section 4 and keep all generated public-repo artifacts proprietary-first per `docs/RIGHTS_PROVENANCE.md`.
