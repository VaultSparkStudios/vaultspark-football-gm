# Closeout Brief — franchise-architect — S106

> Three sessions optimised the consumers of potential-minus-overall. The two numbers were never measured on the same scale — and the fix for that was measured, re-fixed, measured again and refused at a gate it could not satisfy honestly.

## Shipped

- **MEASURED AND REVERTED: potential is drawn on a scale overall does not share** (6/10 project, 5/10 ecosystem): randomPotential drew one band for every position (SUPERSTAR 84-98, HIDDEN 76-94, BUST 58-76, else 68-90) while overall is a position-WEIGHTED average of position-BIASED attributes, so only one of the two numbers knew what position the player plays - and development, reversion, scouting and elite density all read their difference. Measured at generation across four seeds and 8,832 rostered players: mean potential ~80 in every room against mean overall 73.6 (TE) to 81.7 (QB); 36.8 per cent of the league generated ABOVE its own potential (QB 51.8, OL 49.0, TE 22.7); and all 45 players at 90+ in a fresh league were quarterbacks (11) or offensive linemen (34), none anywhere else. The fix - trait-sized headroom above the player's own overall, tapered to nothing by age 31, damped as he nears the 99 ceiling, and drawn in the SAME stream slot as before (one rng.int) so no seeded league's downstream draws shift. Generated above own potential 36.8 per cent to 0.0; mean potential tracks the room (QB 85.8, OL 84.0, TE 77.2); every mean overall unchanged to the decimal, which is the evidence the stream held. REVERTED. test/session90-development-environment.test.js asserts one offseason moves the league by its declared curve and nothing else, and read 0.285 against a 0.25 tolerance - then 0.289 after the obvious follow-up fix, per-room trait centres, was also implemented and measured. Reversion's mean contribution measured exactly 0.000 and the engine's measured trait centre made the gap slightly worse, so the residual is structural: deltas land on ratings and overall is recomputed with position weights, so changing WHICH players move changes aggregate OVR at zero mean delta. Closing the gate would have meant duplicating the engine inside the test or widening a tolerance; both refused. src/ differs from HEAD by 37 lines, all comment, verified by diff.
- **The counterweight discovery, kept as a measurement** (7/10 project, 7/10 ecosystem): Position-aware potential ALONE read elite density 5.3 per cent (out-of-range, from 2.8 watch) and dispersion drift 0.157 (out-of-range, from 0.095) over ten seasons on the canonical seed. The defect had been load-bearing: a third of the league sitting above its own potential was a standing DOWNWARD pull in the reversion term, and the two rooms whose ceilings rose most are exactly the ones that feed elite density - the S90 subsidy and the S104 teamNeeds floor for a third time. Damping headroom by proximity to the ceiling restores a brake defensible on its own terms (a player at 70 keeps nearly all his room to grow, one at 92 keeps a fraction). Shipped reading: elite 1.8 per cent, dispersion 0.083, parity +0.034 - all three better than the session inherited, with no threshold moved and both watch arms still reported as watch.
- **The ledger-budget gate gets its population** (6/10 project, 7/10 ecosystem): The gate asserted four ledgers stayed under 96 KB while logs/WORK_LOG.md (213 KB) and context/TASK_BOARD.md (206 KB) were not in the list at all - and ledger-roll's own header has named TASK_BOARD append-only since S94. Both are registered with their own directory, order and heading patterns (WORK_LOG carries two heading eras; TASK_BOARD is newest-first with standing Now/Next sections that stay live above the first entry) and rolled: 572 KB to 244 KB with S105 entries intact. A new test derives the population from disk - any context/ or logs/ markdown with twelve or more dated session headings must be registered - so the list cannot silently omit a ledger again. Third undeclared-population defect in this project's gates.
- **The brief reports the session authority that exists on disk** (4/10 project, 5/10 ecosystem): render-startup-brief resolved the authority once and self-healed PROJECT_STATUS.currentSession later in the same run, so the rendered line described the pre-heal state: at S105 closeout it healed 104 to 105 and emitted status=S104 divergent=true in the same pass. The authority is now re-resolved immediately after the heal writes, with a behavioural test (a lagging status diverges, the healed one does not) and a structural test asserting the recompute sits between the heal and the emitted comment.
- **A doctor remedy prescribing a contractually forbidden file is deleted** (3/10 project, 6/10 ecosystem): doctor-remedies mapped last-session-summary to check-last-session-summary.mjs --fix. The authority contract forbids that path by name, S104 deleted the script after the studio shard rejected it as a fourth mutator of PROJECT_STATUS.json, and doctor.mjs defines no such check here - so it was inert advice to violate a contract, the trap S104 recorded as a decision still printed as guidance. Removed with the history in its place, and a test asserts no remedy names a forbiddenFiles basename.
- **Ledger ordering is guarded, because this session broke it in four files at once** (5/10 project, 5/10 ecosystem): The S106 entry was appended with the previous session's heading as its anchor, which inserted it ABOVE that heading in SELF_IMPROVEMENT_LOOP, WORK_LOG, DECISIONS and TRUTH_AUDIT: every file ended S104, S106, S105. Retention keeps the LAST entries, so the next roll would have archived the wrong end - the S105 defect wearing a different hat. Moved verbatim to the end of each file, and a test now fails when a newest-last ledger does not end with its highest-numbered entry.
- **The status page says nothing changed, because nothing did** (4/10 project, 3/10 ecosystem): Two gates reported the same defect: status.html's newest release note sat at session 104 against a lastSession of 106, two unpublished against a tolerance of one. Neither S105 nor S106 shipped a player-facing surface, and the gate counts sessions rather than features - so the note written says exactly that in players' terms, records that the ceiling-scale repair was built, measured twice and withdrawn, and states that saved franchises are untouched. A page that only speaks when there is something to boast about is not a status page.

## Follow-ups

- **The next item is the TEST, not the generator**: session90-development-environment compares a scalar declared curve (ageFactor plus trait) against an engine that applies deltas to ratings and recomputes overall with position weights. It cannot express position-relative potential, which is why a correct generator change and a correct follow-up fix both left it red. Make the declared curve model that application, keep the +0.84 S90 subsidy as its negative control, and only then re-land the generator change - whose finding is measured across four seeds and whose calibration measured better on all three distribution arms.
- **Elite density: the remaining question is the population, not the number**: 1.8 per cent against a sourced 1.53 per cent All-Pro ceiling is the closest this project has measured (S91 2.07, S102 3.5, S103 3.3, S105 2.8). The residual is that the 90+ cut is position-blind while First-Team All-Pro is allocated per position - the declared-population question S102-S104 settled for the parity gate, in a new place. State the question before changing the statistic, and never adopt a per-position reading as a route to a green gate.
- **Ledger retention is positional, so the working set is three entries short of what it advertises**: The live SIL still holds S78/S79/S80 and WORK_LOG still holds S87/S95/S96, because retention keeps the last N entries in FILE order and these files have never been strictly ordered. Nothing is lost. Retention by session number means reordering within a live file - a history rewrite that deserves its own session.
- **Cross-seed confirmation of the new generation shape has not been run**: The ten-season readings are one seed (20260306). The generation table is four seeds with no simulation. A cross-seed ten-season run would confirm the calibration holds beyond the canonical path.

## Blockers

- **Public launch remains HOLD — unchanged and untouched this session**: Delivered reply-capable project-domain email, candidate-bound founder launch approval and authoritative lifecycle reconciliation remain three independent unmet gates. launchReady stays false.
- **Authoritative registry still reads SPARKED against a local contract of FORGE**: Non-blocking doctor warning, unchanged since S101. Do not resolve by flipping the local contract: three blocking lifecycle checks would become silent auto-passes.

## Honesty Ledger

- **The session's headline work was refused at a gate and reverted, and every number here measures code that is not live**: Position-aware potential measured better on all three distribution arms and is not in the shipped code. The gate it could not satisfy was not written by this session, the follow-up fix did not move it, and the two available ways to close it were duplicating the engine inside the test or widening a tolerance. The measurement is the deliverable; the revert is verified by diff rather than asserted.
- **The first fix was shipped-quality and wrong, and the only thing that said so was the ten-season run**: Position-aware potential is correct in isolation and took two gates out of range. Nothing in code review would have caught it; the defect it removed was a brake. Recorded as the measurement it was, with the out-of-range numbers published rather than the corrected ones alone.
- **This session broke ledger ordering in four files at once**: The S106 entry was appended above S105 in SIL, WORK_LOG, DECISIONS and TRUTH_AUDIT, because each edit anchored on the previous session's heading. Retention keeps the LAST entries, so the next roll would have archived the wrong end - the S105 defect wearing a different hat. Moved verbatim to the end of each file and guarded by a test that fails when a newest-last ledger does not end with its highest-numbered entry.
- **The premise checker caught two of my own typed premises**: Both matched explanatory comments rather than the code they described - one naming TASK_BOARD in a header comment, one naming the forbidden script inside the comment recording its removal. Re-typed against the code, not the prose about it.
- **Measurement scope declared rather than implied**: Generation figures are four seeds (20260306 / 2026 / 8121 / 4242) with no simulation. The ten-season distribution and parity figures are one seed on the canonical path. No cross-seed ten-season confirmation was run. SIL scores are engineering assessments, not measured player outcomes; no real-cohort evidence exists and none was manufactured.

## Proof

- Files changed: 21
- Insertions: 3344
- Deletions: 2857
- Suite: canonical Node 1,463/1,463 across six shards (core 245, runtime 817, sim-contract 83, sim-realism 1, long 5, studio 312), up from 1,458/1,458 in S105. The full run read 1,461/1,463 with two self-caused studio reds - one defect from two gates, the status page's release note two sessions behind against a tolerance of one - fixed at source and the studio shard re-run green. An earlier full run this session, before the generator revert, read 1,450/1,452 and is superseded.
