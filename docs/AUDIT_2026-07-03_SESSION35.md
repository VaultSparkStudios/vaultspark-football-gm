<!-- generated-by: /audit skill v2 live-code -->
<!-- generated-at: 2026-07-03 -->
<!-- project: franchise-architect-football · type: game/public-product -->

# Project Audit - Franchise Architect: Football Session 35

Fresh `/arc` audit after confirming `.cache/genius-list.json` is exhausted and Session 34's two-item queue is source-shipped. Premises were checked against live code, current task records, the game/public product lens, and the existing shared modal accessibility contract established in Session 32.

## Top-line

- Total ranked items: 1 primary + 1 second-order innovation candidate
- Combined priority: 34.4
- Top item: modal-contract-completion
- Game-loop read: loop tightness remains strong, but high-emotion decision/ritual overlays are part of the core session loop and must not trap keyboard users inconsistently.
- Release gate read: no SPARKED flip attempted; launch remains evidence-blocked on real contact-email receipt plus current live origin/routing proof.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | 🔥 | Accessibility / retention loop | 1.25h | 8 | 4 | 24.0 | **modal-contract-completion** — Session 32 created `modalManager.js`, but live-code inspection showed several high-frequency overlays still bypassed it: Season Review, Pre-Game Tactical Brief, Draft Pick Reveal, Franchise Moment, GM Decision, Agent Negotiation, and Keyboard Shortcuts. Several also lacked dialog semantics in `game.html`. **Recipe:** wire every show/close path through `openModal()` / `closeModal()`, preserve existing promise/choice behavior, add dialog roles/labels, and add regression coverage. |
| 2 | ⚡ | Second-order consistency | 0.25h | 5 | 2 | 10.4 | **priority-inbox-modal-truth** — the Priority Inbox drawer already declared `role="dialog"` and `aria-modal="true"` but did not trap focus or restore focus on close. **Recipe:** treat it as a real modal surface or stop claiming it is modal. The correct fix is to use the same shared manager because it is a focused, dismissible GM inbox drawer. |

## Skipped / Rejected

- **latest-audit-follow-through** — rejected on live evidence: `.cache/genius-list.json` marks `AUDIT_2026-07-03_SESSION34.md` exhausted, and Session 34's execution log shows both ranked items shipped.
- **sparked-flip** — still blocked on real received-message proof that `football@playfranchisearchitect.com` forwards/copies to Studio operations plus current live origin/routing evidence. Route reachability alone is not launch proof.
- **GAME_LOOP.md review** — no `context/GAME_LOOP.md` exists in this public repo; game-loop findings were derived from shipped UI/game-flow code and current state instead of fabricating a design-doc score.

## Execution Log

| Item | Status | Evidence | Verification |
|---|---|---|---|
| modal-contract-completion | Shipped | `public/lib/gameFlow.js`, `public/lib/tabDraft.js`, `public/lib/tabSettings.js`, `public/lib/tabContracts.js`, `public/lib/engagementFeatures.js`, `public/app.js`, and `public/game.html` now route key dialogs through the shared modal lifecycle and expose dialog semantics where missing. | `node --check` on edited modules; `node --test test/browser-wiring.test.js`; `node --test test/modal-manager.test.js` |
| priority-inbox-modal-truth | Shipped | `openInbox()` now calls `openModal(drawer, { onClose: closeInbox })`; `closeInbox()` calls `closeModal(drawer)` before hiding the drawer state. | `node --test test/browser-wiring.test.js` |

---
*Generated by live `/audit` pass · ready for `/closeout` after full validation.*