# Audit — Franchise Architect: Football — Session 60

Public-safe live-code audit. The JSON sidecar is the sole source of truth.

## Profile and review lens

- Product: public-unlaunched public browser football game/app
- Rubric: infrastructure rubric with product, browser-game, and public-app release lenses; staging: GitHub Pages artifact plus exact same-origin hosted verification
- Profile source: live source, direct focused tests, canonical secrets/status probes, CI and release evidence, game-loop contract, and public-safe context
- Game-loop review: tightness 9 · progression 8.4 · session engagement 8.6 · retention 7.8 · soul fidelity 9.2 · overall 8.6
- Evidence caveat: Structural implementation review plus 29/29 targeted executable contracts. Outcome confidence remains low-medium because no consented docs/PLAYTESTS cohort exists.

## Ranked implementation plan

| Rank | Tier | Category | Effort | Impact | Innovation | Priority | Item and concrete recipe |
|---:|---|---|---:|---:|---:|---:|---|
| 1 | FIRE | Security / credential truth / observability | 1.5h | 10 | 7 | 38.7 | **canonical-capability-map-authority** — Resolve map authority with explicit local-override/canonical-fallback precedence, preserve env merging and redaction, expose only status metadata, add temp-directory tests for local override, sibling fallback, named readiness, and corrupt-map fail-loud behavior, then rerun real status-only probes. |
| 2 | FIRE | Game depth / progression / explainable intelligence | 5.0h | 10 | 9 | 32.1 | **player-authored-architect-thesis** — Create one versioned per-team Architect Thesis authority with selected mastery focus and optional Reinforce/Counter/Investigate hypothesis, validate and persist it through both adapters, render accessible controls in Architecture Review, surface it in rehearsal, consume it into the next committed ledger entry, resolve it descriptively from observed film, and retain auto-lowest only as a labeled recommendation. |
| 3 | FIRE | Infrastructure / startup truth / lifecycle coherence | 2.5h | 9 | 7 | 29.0 | **startup-brief-authority-drift-contract** — Reuse lifecycle-coherence as the authority join, render local and registry statuses plus Ark-only drift warning, age-gate the cached project profile, render exhausted cache source/closed count/reason, extend brief freshness to reject changed lifecycle or queue fingerprints, and cover all states with pure and CLI tests. |
| 4 | HIGH | Infrastructure / model routing / observability | 2.0h | 8 | 6 | 24.0 | **context-meter-model-authority** — Export pure model-price and token-cost helpers from the router, consume them in context-meter, extract a pure recommendation function, and fixture-test 50/75/95 percent boundaries, Sonnet guardrail, context overrides, per-model ledger cost, Max Plan notional labeling, and process exit codes. |
| 5 | HIGH | Infrastructure / closeout process / test depth | 2.5h | 7 | 5 | 16.1 | **closeout-board-truth-fixtures** — Add Windows-safe temp-repo CLI fixtures for clean/synchronized, dirty, ahead/unpushed, missing or stale verification receipts, malformed inputs, and dry-run/no-write behavior; assert both rendered fields and validator acceptance without network access. |

Combined priority: **139.9**.

## Premise verification and rejected phantom work

- Rejected/deferred “Make launch green from this repository”: Rejected. The owning canonical host, real mailbox receipt, founder approval, and sibling registry are independent authorities; current-repo reports cannot manufacture them.
- Rejected/deferred “Decompose the 5,395-line GameSession in one pass”: The premise is real, but a one-pass rewrite would exceed a safe bounded seam. A future audit should specify domain order, compatibility facade, and characterization gates before implementation.
- Rejected/deferred “Unify both 120-route adapters immediately”: The duplicated authority is real, but full consolidation is unsafe without incremental endpoint-family migration under exact parity tests.
- Rejected/deferred “Check in fabricated playtest outcomes”: Rejected. Instrumentation exists, but no consented 8–12-player cohort exists; structural scores are not player-outcome evidence.
- Rejected/deferred “Upgrade Playwright 1.61.1 to 1.62.0”: Deferred as low leverage; any future bump must pass package-trust before installation and the full UI evidence suite afterward.

## Three recommended design moves

1. Make every repo-local credential verdict derive its capability shape from the canonical gateway instead of reporting a phantom 0/0 world.
2. Turn inferred Architect adaptation and auto-lowest mastery focus into one explicit, persisted player-authored thesis without hidden bonuses.
3. Make the sole startup brief surface lifecycle authority drift, stale profile evidence, and an honestly exhausted genius queue.

## Execution Log

| Item | Status | Evidence |
|---|---|---|
| canonical-capability-map-authority | done | repo-local --audit lists canonical capabilities instead of 0/0; brevo and cloudflare.dns status matches the canonical checker without printing values; local maps remain explicit higher-precedence overrides; corrupt present maps still fail loud; focused gateway tests run with isolated fixture directories |
| player-authored-architect-thesis | done | versioned per-team thesis authority persists selected focus and source-bound Reinforce/Counter/Investigate hypotheses; auto-lowest remains separately labeled as the recommendation; rehearsal and the next committed ledger carry the declaration; resolution is observation-only with an explicit non-causation disclaimer; invalid and no-film mutations fail closed; focused domain, mastery, rehearsal, browser-wiring, browser-module, and live server/static parity checks pass |
| startup-brief-authority-drift-contract | done | startup brief names local FORGE and registry SPARKED without choosing a false winner; stale profile metadata is labeled stale or omitted; exhausted genius cache renders source, closed count, and reason; freshness changes when lifecycle or genius authority changes; canonical brief validator remains green |
| context-meter-model-authority | done | no model/context/price constant is duplicated in context-meter; router helpers cover exact IDs and safe tier fallbacks; verdict vocabulary and exit codes remain canonical; Max Plan dollars remain explicitly notional and never trigger an alarm; focused boundary tests and live context-meter JSON pass |
| closeout-board-truth-fixtures | done | fixtures create isolated git repositories and never touch the working repo; dirty and ahead states cannot render synchronized/clean; missing or stale receipts remain visibly non-green; candidate boards pass the canonical format validator; tests use the hidden-window safe spawner |
