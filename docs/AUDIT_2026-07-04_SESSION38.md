<!-- generated-by: /audit skill v2 live-code -->
<!-- generated-at: 2026-07-04 -->
<!-- project: franchise-architect-football · type: game/public-product -->

# Project Audit - Franchise Architect: Football Session 38

Fresh `/goal /arc` audit after recovery closeout and startup. Premises were checked against live mobile loop code, the desktop advance-week gate, `/api/gm-decision`, return digest wiring, and the game/public product lens.

## Top-line

- Total ranked items: 2 shipped items, including 1 second-order innovation candidate
- Combined priority: 43.4
- Top item: mobile-gm-decision-first
- Game-loop read: the narrow-screen loop now surfaced pressure well, but a live pending General Manager decision could still hide behind a generic Advance Week action while desktop correctly gates the advance flow.
- Release gate read: no SPARKED flip attempted; launch remains blocked on real received-message proof for `football@playfranchisearchitect.com` forwarding/copying.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | 🔥 | Mobile core loop / decision clarity | 0.8h | 8 | 6 | 29.6 | **mobile-gm-decision-first** — live-code inspection showed desktop calls `checkAndShowGmDecision()` before advance, but mobile mode rendered generic decision cards from dashboard/news only. **Recipe:** carry the first pending `/api/gm-decision` result into mobile state and render it as the first mobile decision card before lower-priority pressure. |
| 2 | ✨ | Feedback loop / live state sync | 0.4h | 6 | 4 | 13.8 | **mobile-gm-decision-refresh-affordance** — a static card would become stale after dashboard changes. **Recipe:** when mobile mode is active in regular season, fetch `/api/gm-decision`, store the first pending decision, and re-render the overlay without inventing parallel decision logic. |

## Game-loop Review

| Axis | Score | Finding |
|---|---:|---|
| Loop tightness | 10/10 | Mobile now compresses state -> pending decision -> pressure -> action, matching the desktop gate. |
| Progression curve | 9/10 | GM decision prompts remain tied to season timing instead of becoming a generic advance habit. |
| Session engagement | 10/10 | Returning on a phone starts with the highest-stakes unresolved choice when one exists. |
| Retention hooks | 9/10 | Decision-first mobile complements return digest, owner pressure, fan pulse, and replay without fabricated telemetry. |
| Soul fidelity | 10/10 | The product feels like a front-office simulator by making the General Manager answer the live football choice before simming forward. |

## Skipped / Rejected

- **sparked-flip** — still blocked on real received-message proof that `football@playfranchisearchitect.com` forwards/copies to Studio operations. No fabricated launch evidence used.
- **GAME_LOOP.md fabrication** — no `context/GAME_LOOP.md` exists in this public repo; scores above are derived from live code and current task/state records.
- **new backend decision engine** — rejected as unnecessary; `/api/gm-decision` already exists and desktop already trusts it.

## Execution Log

| Item | Status | Evidence | Verification |
|---|---|---|---|
| mobile-gm-decision-first | Shipped | `state.mobilePendingDecision` now feeds `buildMobileDecisionDeck()`, which renders a pending GM decision as the first mobile decision card with an advance-week action that reuses the desktop decision gate. | `node --check public/lib/mobileLoop.js`; `node --test test/mobile-loop.test.js` |
| mobile-gm-decision-refresh-affordance | Shipped | `syncMobileLoopOverlay()` fetches `/api/gm-decision` when mobile mode is active in regular season, stores the first decision, and re-renders the overlay from the same state. | `node --check public/app.js`; `node --test test/mobile-loop.test.js` |

---
*Generated by live `/audit` pass · implementation shipped in the same `/arc` mission.*