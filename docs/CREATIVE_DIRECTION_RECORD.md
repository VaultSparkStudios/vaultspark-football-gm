# Creative Direction Record

This public repo now keeps only public-safe creative-direction summaries.

Boundary:
- detailed private creative direction and internal rationale live in the private Studio OS / ops repository

## 2026-05-27 — Project-specific workaround preference

Direction captured: do not solve a single-project Codex Apps startup failure by disabling Apps globally, because the broader Studio portfolio may need that capability.

Public-safe interpretation: runtime workarounds should be scoped to the affected project unless there is clear evidence that a global setting is safe for every project.

## 2026-05-27 — Closeout completeness expectation

Direction captured: when the founder asks for closeout, commit, and push, update memory, context, CDR, task board, and any related project state rather than only committing code.

Public-safe interpretation: closeout is a full state synchronization step. Public-safe truth surfaces should match the pushed code state before the session is considered done.
