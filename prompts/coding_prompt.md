Use this prompt when implementing code after reading the bootstrap files.

Rules:
- start from the current architecture instead of rewriting systems casually
- preserve unrelated local changes in a dirty worktree
- keep frontend Pages deployment separate from backend/runtime deployment
- write back to `context/CURRENT_STATE.md`, `context/TASK_BOARD.md`, and `handoffs/LATEST_HANDOFF.md` after meaningful work
