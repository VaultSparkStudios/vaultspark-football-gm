<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-15 -->
<!-- project: vaultspark-football-gm · type: game -->

# Project Audit — VaultSpark Football GM

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 92.6
- Top 3: mobile-gm-decision-deck · feedback-franchise-fingerprint · closeout-protocol-shims

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | ui/ux/user-experience | 1h | 9 | 7 | 39.8 | **mobile-gm-decision-deck** — Make the mobile loop a real General Manager decision deck. The mobile overlay already compresses the franchise into a single-column view, but its action row is static and misses the highest-pressure next decision. A narrow beta tester should see the next meaningful General Manager move immediately. **Recipe:** Add a pure `buildMobileDecisionDeck()` helper, render its top actions in `renderMobileOverlay()`, and cover draft, cap/injury, and default states. |
| 2 | ⚡ | user feedback loop | 45m | 8 | 6 | 32.8 | **feedback-franchise-fingerprint** — Attach a compact franchise fingerprint to beta feedback. The beta issue URL now carries launch readiness, but it still loses the franchise state that explains why a report matters. A privacy-safe fingerprint lets feedback be triaged without analytics or accounts. **Recipe:** Add `buildFeedbackContextFingerprint()`, include optional fingerprint lines in `buildFeedbackIssueUrl()`, gather it in `openFeedback()`, and test that no secret-like or save payload data leaks. |
| 3 | ⚡ | speed/organization/efficiency | 1h | 7 | 5 | 20.0 | **closeout-protocol-shims** — Backfill missing closeout protocol scripts locally. Startup has repo-local shims, but closeout still references missing cost and impact-brief scripts, creating avoidable protocol drift. **Recipe:** Add `scripts/record-skill-cost.mjs` and `scripts/render-closeout-brief.mjs` with public-safe behavior plus tests for expected artifacts. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- 0 items.

## Skipped (Priority < 1.5)

- **cloudflare-agent-remediation** — Still requires Cloudflare/GitHub org-root capability that is missing from this repo; keep it surfaced as an agent-attemptable blocker with the existing runbook.
- **ai-scouting-rival-coach** — Best deferred until beta feedback produces real decision traces; otherwise the critique model would invent player habits.

---
*Generated by `/audit` v1.0 · ready for `/implement`*

## Execution Log

- **mobile-gm-decision-deck** — shipped 2026-06-15. Added `buildMobileDecisionDeck()`, rendered prioritized mobile decision cards in the overlay, routed cards to existing tabs or advance-week, and covered draft/cap-injury/default states. Verification: focused mobile tests `3/3`; runtime shard `79/79`.
- **feedback-franchise-fingerprint** — shipped 2026-06-15. Added `buildFeedbackContextFingerprint()`, attached team/record/cap/top-need/pressure rows to beta issue URLs, gathered the fingerprint from live state, and verified no secret-like/save payload fields leak. Verification: focused beta feedback tests `6/6`; runtime shard `79/79`.
- **closeout-protocol-shims** — shipped 2026-06-15. Added public-safe `record-skill-cost` and `render-closeout-brief` shims with temporary-directory renderer coverage. Verification: studio protocol shard `5/5`.
