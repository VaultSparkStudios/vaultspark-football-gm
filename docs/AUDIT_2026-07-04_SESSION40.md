<!-- generated-by: /audit skill v2 live-code -->
<!-- generated-at: 2026-07-04 -->
<!-- project: franchise-architect-football � type: game/public-product -->

# Project Audit - Franchise Architect: Football Session 40

Fresh `/goal /arc` audit after the Session 39 genius cache was exhausted. Premises were checked against the live mobile overlay refresh code, the pending `/api/gm-decision` source, and the existing mobile-loop regression tests.

## Top-line

- Total ranked items: 1 shipped item
- Combined priority: 36.8
- Top item: mobile-gm-decision-snapshot-guard
- Game-loop read: mobile mode already lets a phone user answer the pending General Manager decision inline; the remaining honesty gap was stale asynchronous decision refresh.
- Release gate read: no SPARKED flip attempted; launch remains blocked on real received-message proof for `football@playfranchisearchitect.com` forwarding/copying and current live-origin/routing evidence.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | FIRE | Mobile core loop / source-of-truth honesty | 0.7h | 8 | 8 | 36.8 | **mobile-gm-decision-snapshot-guard** � the mobile overlay fetched `/api/gm-decision` after rendering, but did not prove the returned decision still matched the current team/week/phase and did not repaint after clearing stale state on failure. **Recipe:** derive a compact dashboard snapshot key before fetch, ignore stale responses, clear/repaint on fetch failure, and cover the contract in `test/mobile-loop.test.js`. |

## Game-loop Review

| Axis | Score | Finding |
|---|---:|---|
| Loop tightness | 10/10 | The phone loop now keeps pressure -> choice -> consequence aligned to the current dashboard snapshot. |
| Progression curve | 9/10 | Decisions remain tied to the current regular-season week and existing consequence path. |
| Session engagement | 10/10 | Returning mobile users avoid stale or ghost decision cards after network/runtime churn. |
| Retention hooks | 9/10 | Mobile decision trust improves the already strong return-digest, inbox, fan pulse, and owner-pressure hooks. |
| Soul fidelity | 10/10 | The General Manager desk shows the live decision in front of you, not a stale one from a previous state. |

## Skipped / Rejected

- **duplicate-mobile-decision-cache** � rejected because `state.mobilePendingDecision` and `/api/gm-decision` already provide the source-of-truth path; adding a second cache would make the surface less honest.
- **sparked-flip** � still blocked on real received-message proof that `football@playfranchisearchitect.com` forwards/copies to Studio operations plus current live-origin/routing evidence. No fabricated launch evidence used.

## Execution Log

| Item | Status | Evidence | Verification |
|---|---|---|---|
| mobile-gm-decision-snapshot-guard | Shipped | `syncMobileLoopOverlay()` now captures a dashboard snapshot key before `/api/gm-decision`, ignores stale async responses, clears stale pending decisions on failure, and repaints the mobile overlay from the current state. | `node --check public/lib/mobileLoop.js`; `node --check public/app.js`; `node --test test/mobile-loop.test.js` 12/12; broader suite in `/closeout`. |

---
*Generated by live `/audit` pass � implementation shipped in the same `/arc` mission.*

