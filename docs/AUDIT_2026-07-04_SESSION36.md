<!-- generated-by: /audit skill v2 live-code -->
<!-- generated-at: 2026-07-04 -->
<!-- project: franchise-architect-football · type: game/public-product -->

# Project Audit - Franchise Architect: Football Session 36

Fresh `/goal /arc` audit after startup confirmed Session 35's genius cache was exhausted. Premises were checked against live code, current launch evidence, current task records, and the game/public product lens.

## Top-line

- Total ranked items: 1 primary item; 1 second-order candidate rejected on evidence
- Combined priority: 20.2
- Top item: tutorial-theme-token-parity
- Game-loop read: first-session onboarding is part of the core loop; it must inherit the same light/dark/accent contract as the rest of the product, not regress to a hard-coded dark modal after the theme repair arc.
- Release gate read: no SPARKED flip attempted; live route checks are green, but launch remains blocked on real received-message proof for `football@playfranchisearchitect.com` forwarding/copying.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | 🔥 | Onboarding / theme parity | 0.6h | 7 | 4 | 20.2 | **tutorial-theme-token-parity** — live-code inspection showed `public/lib/tutorialCampaign.js` still injected hard-coded dark tutorial overlay/modal/choice colors (`#111820`, `#182028`, `#f5f1e7`, etc.) even though the app now has a tokenized light/dark/accent theme system. First-run players in light mode could hit a dark, off-system tutorial as their first game-loop moment. **Recipe:** route tutorial overlay, modal, text, choices, progress, and selected states through existing theme tokens; add Playwright coverage proving the first-run tutorial is readable in light mode. |

## Game-loop Review

| Axis | Score | Finding |
|---|---:|---|
| Loop tightness | 9/10 | Advance-week, inbox, decisions, and season rituals remain cohesive; no new loop break found. |
| Progression curve | 9/10 | Long-save systems and tutorial choices introduce pressure without adding backend cost. |
| Session engagement | 9/10 | First-run tutorial is a high-leverage engagement surface; theme mismatch was the live weakness. |
| Retention hooks | 9/10 | Return digest, inbox, what-if replay, and saves remain source-shipped; no fabricated analytics. |
| Soul fidelity | 9/10 | Public-safe SOUL is minimal, but the shipped direction stays browser-native, truthful, and franchise-focused. |

## Skipped / Rejected

- **latest-audit-follow-through** — rejected on evidence: `.cache/genius-list.json` marks `AUDIT_2026-07-03_SESSION35.md` exhausted and the Session 35 execution log/source tests prove both ranked items shipped.
- **sparked-flip** — still blocked on real received-message proof that `football@playfranchisearchitect.com` forwards/copies to Studio operations. `scripts/launch-evidence-report.mjs --json` on 2026-07-04 showed routesOk=true and emailForwardingVerified=false.
- **GAME_LOOP.md fabrication** — no `context/GAME_LOOP.md` exists in this public repo; scores above are derived from live code and current task/state records.

## Execution Log

| Item | Status | Evidence | Verification |
|---|---|---|---|
| tutorial-theme-token-parity | Shipped | `public/lib/tutorialCampaign.js` tutorial CSS now uses `--scrim`, `--panel-grad`, `--card-grad`, `--ink`, `--ink-strong`, `--muted`, `--line`, `--accent`, and related tokens instead of hard-coded dark colors. | `node --check public/lib/tutorialCampaign.js`; `npx playwright test tests-ui/theme.spec.js` |
| tutorial-light-readability-regression | Shipped | `tests-ui/theme.spec.js` adds a first-run tutorial light-theme regression that creates a new league, waits for the tutorial, and verifies light surfaces plus dark text contrast. | `npx playwright test tests-ui/theme.spec.js` |

---
*Generated by live `/audit` pass · ready for full validation and `/closeout`.*
