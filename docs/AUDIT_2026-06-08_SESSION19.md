<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-08 -->
<!-- project: vaultspark-football-gm · type: game -->

# Project Audit — VaultSpark Football GM

> One ranked improvement plan focused on the next beta-readiness layer after Session 18.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 92.6
- Top 3: mobile-gm-decision-deck · feedback-franchise-fingerprint · closeout-protocol-shims

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | ui/ux/user-experience | 1h | 9 | 7 | 39.8 | **mobile-gm-decision-deck** — Make the mobile loop a real General Manager decision deck. The mobile overlay already compresses the franchise into a single-column view, but its action row is static and misses the highest-pressure next decision. **Recipe:** add a pure `buildMobileDecisionDeck()` helper that derives two to three prioritized mobile actions from phase, week, cap, injuries, draft, roster needs, and news; render those actions in `renderMobileOverlay()`; add focused tests for draft, cap/injury, and default advance-week states. |
| 2 | ⚡ | user feedback loop | 45m | 8 | 6 | 32.8 | **feedback-franchise-fingerprint** — Attach a compact franchise fingerprint to beta feedback. The beta issue URL now carries launch readiness, but it still loses the franchise state that explains why a report matters. **Recipe:** add `buildFeedbackContextFingerprint()` as a pure helper, include optional fingerprint lines in `buildFeedbackIssueUrl()`, gather the snapshot in `openFeedback()`, and test that the body includes gameplay context without tokens, emails, or save payloads. |
| 3 | ⚡ | speed/organization/efficiency | 1h | 7 | 5 | 20.0 | **closeout-protocol-shims** — Backfill missing closeout protocol scripts locally. Startup has repo-local shims, but closeout still references missing cost and impact-brief scripts, which makes every session carry avoidable protocol drift. **Recipe:** add `scripts/record-skill-cost.mjs` and `scripts/render-closeout-brief.mjs` with public-safe behavior, wire tests that both scripts load and emit/write expected artifacts, and keep outputs secret-redacted and local to `.cache`/`docs`. |

## Innovation reserve (Innovation >= 8)

- None this pass; the strongest novelty is turning the mobile surface into a decision-priority deck without backend cost.

## Skipped

- `cloudflare-agent-remediation` — Cloudflare/GitHub org-root capability remains missing locally, so the repo should keep surfacing the runbook rather than pretending it can mutate the shared domain.
- `ai-scouting-rival-coach` — Defer until beta feedback yields real decision traces.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
