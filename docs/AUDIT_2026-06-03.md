<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-03 -->
<!-- project: vaultspark-football-gm · type: game -->

# Project Audit - VaultSpark Football GM

> One ranked improvement plan covering the highest-leverage current surfaces.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 3
- Combined Priority: 55.6
- Top 3: ci-friendly-test-shards · pages-smoke-deploy-gate · test-surface-contract-map

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|---|---|---|---:|---:|---:|---|
| 1 | fire | speed/organization/efficiency | 2h | 9 | 6 | 22.5 | **ci-friendly-test-shards** - Split the simulation-heavy test suite into actionable shards. The known 20-minute local timeout hides which football surface failed, so shard by contract, runtime, simulation realism, and Studio protocol. **Recipe:** Add a shard runner with named test groups, npm scripts for each shard, and a CI matrix that runs shards independently. |
| 2 | fire | security | 1h | 8 | 5 | 20.0 | **pages-smoke-deploy-gate** - Make the GitHub Pages deploy prove the playable client boots. The deploy path currently builds artifacts; it should also prove the client-only league path boots before publishing. **Recipe:** Run the existing static Pages smoke test in deploy-pages after build and wire CI to execute the same smoke gate after the Pages bundle is produced. |
| 3 | high | token/api consumption reduction | 30m | 7 | 5 | 13.1 | **test-surface-contract-map** - Give future agents a low-token map from change surface to test shard. A compact contract map cuts repeated context and command cost by telling the next agent exactly which shard covers each surface. **Recipe:** Document shard ownership in the implementation plan and package scripts, then log the measurement plan for full-suite timeout replacement. |

## Innovation Reserve

- 0 items with innovation >= 8 in this focused sprint.

## Skipped

- New gameplay systems were deferred because the current blocker is confidence throughput: the game already has rich Session-8 engagement surfaces, but slow undifferentiated verification is throttling every future change.

## Execution Log

- 2026-06-03 - `ci-friendly-test-shards` shipped. Added `scripts/run-test-shard.mjs`, npm shard scripts, default `npm test` composition, and a CI matrix for `core`, `runtime`, `sim:contract`, `sim:realism`, and `studio`. Evidence: `npm test` passed with 131 default tests; `npm run test:long` passed with 3 long-smoke tests.
- 2026-06-03 - `pages-smoke-deploy-gate` shipped. CI and deploy workflows now run `npm run smoke:pages` after building the static Pages bundle and before publish/upload gates. Evidence: `npm run build:pages` and `npm run smoke:pages` passed locally after installing existing lockfile dependencies.
- 2026-06-03 - `test-surface-contract-map` shipped. `docs/IMPLEMENT_PLAN.md` maps product/code surfaces to shard commands and records the measurement plan for replacing the old single-suite timeout. Evidence: the implementation plan names the shard contracts and marks `test:long` as the explicit scheduled/deep confidence path.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
