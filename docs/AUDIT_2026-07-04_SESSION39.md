<!-- generated-by: /audit skill v2 live-code -->
<!-- generated-at: 2026-07-04 -->
<!-- project: franchise-architect-football · type: game/public-product -->

# Project Audit - Franchise Architect: Football Session 39

Fresh `/goal /arc` audit after Session 38 cache exhaustion. Premises were checked against live mobile loop code, the app-shell pending decision refresh, the existing `/api/gm-decision` source, and the `/api/advance-week` `gmDecisionChoice` consequence path.

## Top-line

- Total ranked items: 1 shipped item
- Combined priority: 35.1
- Top item: mobile-inline-gm-decision-choices
- Game-loop read: mobile mode already surfaced the pending General Manager decision first, but it still made phone users bounce through the generic advance path instead of choosing directly in the mobile loop.
- Release gate read: no SPARKED flip attempted; launch remains blocked on real received-message proof for `football@playfranchisearchitect.com` forwarding/copying and current live-origin/routing evidence.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---:|:-:|:-:|:-:|---|
| 1 | 🔥 | Mobile core loop / decision agency | 0.9h | 9 | 7 | 35.1 | **mobile-inline-gm-decision-choices** — pending `/api/gm-decision` prompts were first in the mobile deck, but the card only had `action: "advance-week"`. **Recipe:** render the decision options inline, dispatch the selected choice from the mobile overlay, and submit it through the existing `/api/advance-week` `gmDecisionChoice` path so there is no duplicated backend logic. |

## Game-loop Review

| Axis | Score | Finding |
|---|---:|---|
| Loop tightness | 10/10 | Mobile now compresses pressure -> choice -> consequence without leaving the phone-first loop. |
| Progression curve | 9/10 | Decision prompts remain tied to season state and advance through the established consequence engine. |
| Session engagement | 10/10 | A returning mobile user can answer the live front-office question immediately instead of tapping a generic advance card first. |
| Retention hooks | 9/10 | Inline choices make mobile pressure cards feel actionable, complementing return digest, fan pulse, owner pressure, and inbox hooks. |
| Soul fidelity | 10/10 | The game feels more like a General Manager desk: make the call, then live with the result. |

## Skipped / Rejected

- **new-backend-decision-engine** — rejected because `/api/gm-decision` and `POST /api/advance-week` `gmDecisionChoice` already exist; duplicating them would add drift.
- **sparked-flip** — still blocked on real received-message proof that `football@playfranchisearchitect.com` forwards/copies to Studio operations plus current live-origin/routing evidence. No fabricated launch evidence used.

## Execution Log

| Item | Status | Evidence | Verification |
|---|---|---|---|
| mobile-inline-gm-decision-choices | Shipped | `buildMobileDecisionDeck()` now carries decision metadata and option choices; `renderMobileOverlay()` renders inline option buttons and emits `vsfgm:mobile-gm-decision-choice`; `submitMobileGmDecisionChoice()` posts the selected choice through `/api/advance-week` with `gmDecisionChoice`. | `node --check public/lib/mobileLoop.js`; `node --check public/app.js`; `node --test test/mobile-loop.test.js`; `npm test`; `npm run test:ui`; `npm run build:pages`; `npm run smoke:pages` |

---
*Generated by live `/audit` pass · implementation shipped in the same `/arc` mission.*
