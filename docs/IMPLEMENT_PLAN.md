# Implementation Plan — Session 80 Arc

The audit sidecar `docs/AUDIT_2026-08-11_SESSION80.json` is the source of truth. Work is ordered by shared authority and verification efficiency rather than rank alone.

## Wave 1 — Exact player-action authority

- [ ] `exact-command-surface-routing`: add explicit target IDs, consolidate Command Center/Blueprint/Season chapter navigation behind one tested scroll/focus authority, and preserve tab-island behavior.
- [ ] Verify draft, cap, injury, deadline, league-pulse, and missing-target behavior with focused tests.

## Wave 2 — Dynasty progression payoff

- [ ] `mastery-signature-visibility`: extend the canonical signature with evidence count/status and render the strongest source-derived signature plus honest empty state in Architecture Review.
- [ ] Verify engine, markup, accessibility, and non-causal language contracts.

## Wave 3 — Second-order saturation and integrated proof

- [ ] Generate the innovation pack after the primary items, implement every viable candidate, and reconcile its machine-readable statuses.
- [ ] Run focused and full Node/Playwright suites, Pages build/smoke, browser boot/reachability, public contracts, and Doctor.
- [ ] Inspect dark/light desktop/mobile rendered pixels for every touched state and bind `docs/visual-qa/LATEST.json` to the final candidate.

## Wave 4 — Exact full-stack release and closeout

- [ ] `exact-candidate-full-stack-promotion`: close out and sanitize, commit the immutable candidate locally, prove exact SHA/artifact/rollback on stable staging, then push direct to `main`.
- [ ] Wait for exact production Pages provenance, dispatch the backend workflow with `deploy_to_server=true`, and verify live API/database/CORS/cache health.
- [ ] Preserve `launchReady: false`; refresh audit, Task Board, state, decisions, work log, SIL, truth audit, handoff, release receipts, closeout visuals, Ark cargo, and session lock.
