## 2026-08-16 — Session 87 — Binding Franchise Pressure and Live-Surface Truth

Completed the full `/arc` and shipped all six ranked items from a fresh executable audit. One versioned salary-market curve now governs generated contracts, free-agency offers and CPU bidding; opening leagues carry meaningful cap pressure and can reach the configured $45M salary ceiling. Live chemistry/owner adapters drive narrative events, longest-play statistics retain maxima, fan sentiment reads the current season, waiver projections preserve player identity, and Franchise Legends plus General Manager Reputation are mounted.

Rendered-pixel work generated 233 responsive captures and retained 84 dark/light desktop/mobile receipts. Manual inspection found a real shared-column decorator offset that replaced OVR with the player name; it was fixed and recaptured before publication. Canonical local verification passed 1,136/1,136 Node and 41/41 Playwright, with Pages build/smoke/module/boot checks green.

Immutable application candidate `90f0d4871828fc10df7b0933f636793db2697446` passed exact-SHA CI, stable staging and production Pages promotion at artifact `9bc71a36579a03f2b566ff3e2f0b512a9677b19f8f26b73ea8471c9b31212a59`; the public backend reports database ready at the same SHA. The first authorized server run exposed a post-deploy idempotency bug: containers were healthy and live, but an unconditional reload of unchanged shared Caddy configuration failed the workflow. The workflow now compares the installed fragment and reloads only on a real delta.

Public launch remains NO-GO. Technical deployment authorization did not provide Zoho receive/reply-as evidence, SHA-bound public-launch approval, lifecycle reconciliation, or external Obelisk relying-party proof. CDR reviewed — no new human creative direction was introduced.

## 2026-08-26 — Session 95 — Incremental Postseason, Calibrated Owner Capital, and Exact Production Identity

Completed the full `/arc`: shipped all seven ranked findings plus Hall dashboard and whole-season second-order fixes; passed Node 1,280/1,280, Playwright 54/54, 251 responsive states with 92 retained reviewed captures, stable staging 14/14, and production 10/10; deployed exact SHA `92e4190a4221383c8352ea6321d65528cb449e74` at artifact `ca66c9210c1efbd234c31d9f392ca70dd6aa03171b47c90057b32d02210767e7` via green workflow 32933281222. Public launch remains HOLD.

## 2026-08-26 — Session 96 — Rendered Settlement, Actionable Promotion Proof, and Mobile Parity

Completed the full `/arc` and shipped all four reproduced findings: truthful startup orientation, deterministic per-file promotion diagnostics, repeatable aggregate-only Cloudflare reporting proof, and the 44px/four-edge-safe mobile navigation contract. The rendered-pixel loop caught a global Escape handler waking cold lazy islands; manual pixels caught mid-transition drawer evidence; Linux CI then proved the 320ms wait non-portable. All three were root-fixed, with the final harness polling rendered geometry.

Verification: implementation-boundary canonical Node 1,293/1,293; final-SHA CI 1,290/1,290 across five push shards in workflow 33030325197; Playwright 54/54; focused release/staging checks 26/26 and 19/19; responsive evidence 255 states with all 240 required and 96 retained reviewed captures; stable staging 14/14; production 10/10; hosted performance verified. Exact candidate `93b867e010dfe3773116433e5b8068dc894e7c0c` and artifact `c45c4a49a46da9e0438219744b8e42e30fb96125ba8c414881c665fe766783a6` are live through green promotion workflow 33031371311. Stable staging deployment is `ed3ce135-b32d-4605-a144-a962696f707a` with rollback `7b67032b-358f-4cc1-83f0-d2a5ec28ed29`. Superseded run 33019799404 is recorded because it exposed the wall-clock wait defect.

Cloudflare reports seven aggregate page loads since S94 (six `/`, one `/landing`, zero `/game.html`). That proves reporting only—not people, sessions, engagement, retention, or a cohort. A least-privilege analytics-read capability recommendation was shipped to Studio Ops through Ark receipt `01K103S9CJB19ECBBDE12BC911`.

Final doctor reconciliation caught two release-contract defects after the first promotion: the new per-file ledger made `_health` too large for the launch-evidence read bound, then the verifier expected a full ledger from the intentionally compact replacement. Commits `8dd0743` and `590d145` now keep `_health` at 846 bytes with compact identity fields while the complete 207-file ledger remains in the deploy manifest used for exact binding. Both negative controls pass and the final SHA was re-promoted.

The first documentation closeout commit then exposed three independent repository-contract regressions in CI: the public status page lacked a current release note, the visual capture ledger did not yet register all 96 retained S96 hashes, and the startup-authority test fixture inherited the live newest audit. Commit `93b867e` fixed all three, passed local Studio 262/262 plus focused 10/10, passed every shard of workflow 33030325197, and was rebuilt and promoted as the final technical release. The misrouted Studio closeout autopilot was stopped before commit and reported through Ark receipt `01K10BKC0C4B276631DD0B1360`; this repository was closed manually through the same secret, coherence, CI, and exact-artifact gates.

The Studio closeout autopilot was stopped before commit after its `--project .` path rendered Studio-level Session 306 output and ran Studio-root refresh tasks from this project closeout. The incorrect local generated file was removed, the repo-local startup renderer restored the truthful S97 brief, and the already-validated evidence set was committed manually. The routing defect was returned to Studio Ops through Ark receipt `01K10BKC0C4B276631DD0B1360` rather than repaired by editing the sibling repository.

Public launch remains HOLD: Zoho delivery/reply-as, explicit public-launch approval, lifecycle authority, and applicable external Obelisk relying-party proof remain separate. CDR reviewed; no new creative direction or DECISIONS entry is owed. Shell accounting: 1 started, 1 closed, 0 running.


## 2026-09-09 — Session 99 — Tie-honest debrief and verified deployment

S99 shipped tie-complete results across career and post-game surfaces, focus-safe drawer dismissal, an explicit local-only first-debrief pulse, truthful session/release guidance, and stable mobile entry paint. Exact staging, production Pages and backend deployment are verified; public launch remains HOLD.

Application revision `713b326d4d84843112ddbc8354c0760bb6400859` and artifact `aba5118bdc0c045780d5c8b15c33f02fca46a0bce53b26e8be826520629bb3d4` pass staging 14/14 and production provenance 10/10. CI 34273797142, Pages promotion 34275252182, backend deployment 34275259066, and receipt-only CI 34296387803 succeeded. Live staging/production currency passed 6/6 during closeout recovery; live backend reports database ready at the exact revision. The implementation-boundary Node receipt is 1,319/1,319 and Playwright is 55/55; these are retained historical receipts, not newly rerun suites.

Recovered the incomplete closeout: the clean tree and green release existed while handoff, SIL and session status still described S98. Both audit plans and the second-order private debrief pulse are complete. CDR reviewed: no new human creative direction. No lifecycle flip or public announcement was made.

Closeout verification: Studio shard 267/267 passed; doctor blockingFailing 0 with the existing lifecycle warning; settings sanitizer found zero issues. Entropy measured 0.070 (healthy); the canonical update path rejected the missing local schema, so the measured result was persisted through this repository's invariant-enforcing writer.


## 2026-09-09 — Session 100

S100 fixes storage-safe optional feedback dismissal, canonical once-only player milestone reporting, visible and keyboard-safe news tickers, and consistent completed-audit status handling. Stable staging, production Pages and backend deployment are verified.

Application revision d0fea099ea9c177e724d958755d1d47a93770a3b; artifact ec10055728641c1e7942368fab4b85f6dd57d1cbedf200c0be00f9443d49b2b6. Staging 14/14; production 10/10; hosted performance verified. Node 1327/1327 across six shards, with the studio shard rerun after adding screenshot-ledger entries. Local browser verification: 59/60 on the final full run, then the sole setup timeout passed unchanged on focused rerun; earlier full run 60/60. Hosted candidate workflows: CI 34331844586 success; Deploy Pages 34333215833 success; Deploy Backend Runtime 34333220807 success. Sixteen reviewed captures cover desktop/mobile and both themes.

No new dependency or per-user service cost. CDR reviewed: no new creative direction. Public launch and cohort claims remain unverified.

## 2026-09-09 — Session 101 — Advertised rules become enforced rules

*(Recovered in S102. The S101 closeout wrote every other surface — CURRENT_STATE, TASK_BOARD, HANDOFF, DECISIONS, SIL, TRUTH_AUDIT, PROJECT_STATUS — and its receipts and closeout brief were committed, but this log never received its entry. The record below is reconstructed strictly from S101's own committed artifacts; nothing is inferred beyond them.)*

S101 found that several rules the game advertises were never enforced, and that several gates reporting green had no ability to report red. The trade deadline became one declared league setting enforced at the shared trade command seam, replacing three surfaces that each hand-typed their own window for a rule `TradeService` never checked — and CPU front offices obey it. The salary cap can no longer be walked through by a fallback that caught the cap authority's refusal and hand-wrote the contract with the old cap hit. A restored franchise can no longer become permanently unadvanceable behind a compatibility check that said "compatible". The rewind lane, whose every write exceeded the browser quota at 12.13 MB against a 5-10 MB budget, now goes through the save codec, refuses a restore whose recovery point failed, and rebuilds a corrupt index rather than committing an empty one. Banked cap space carries forward again, coaching contracts run their declared length, and all seven staff roles expire rather than three.

Verification: canonical Node 1,369/1,369 across six shards (core 210, runtime 784, sim-contract 83, sim-realism 1, long 5, studio 286). The studio shard was rerun after two self-caused reds on its first pass — a stale `static/` build whose modulepreload list predated the session's new module, and a Command Center fixture that predated the declared `tradeDeadlineWeek` setting; both were fixed at source. Doctor `blockingFailing 0`. Boot budget 649,152/650,000 static bytes with zero lazy leaks; public-truth green over a population widened from 10 to 102 shipped surfaces. Engine fixes were re-measured over 10 simulated seasons: cap rollover holds ~29-31M where it previously collapsed to ~1.09M by season 8, with `capComplianceUnresolved` empty every season.

Deployment receipts and reconciled release authority were recorded at the deployed revision. Public launch remains HOLD.


## 2026-09-10 — Session 102 — The ledger, the pool, and the population a gate declares

S102 closed the measured engine backlog S101 deferred and found four more defects while measuring it. Canonical Node **1,415/1,415** across six shards (core 235, runtime 805, sim-contract 83, sim-realism 1, long 5, studio 286), up from 1,369; doctor `blockingFailing 0` with the one standing lifecycle-registry warning.

A contract's cap hit climbed every year it survived — measured +23.0% over 3 years, +26.6% over 4, +28.9% over 5, with the final year always the most expensive and always the full salary — because `normalizeContract` computed the bonus-amortization term into a local and discarded it, leaving every reader to re-derive proration from the years remaining. The term is persisted; legacy saves migrate to their remaining term plus void years and stop accelerating at the load, additively and with no snapshot schema break. Releasing a player now accelerates all remaining proration rather than one year of it, June 1 splits real multi-year money, and the fifth-year option stopped being free (`capHit` arrived through an object spread and was never recomputed). The free-agent pool got an exit: three consecutive unsigned offseasons ends a career, applied deterministically while still consuming the retirement roll so no seeded league is re-calibrated — 400 unsigned players without the rule against 296 with it, seed 2026 at simulated season 10, same code.

The headline was a gate measuring two leagues at once. `LEAGUE_DISTRIBUTION_TARGET` has declared "active-roster overall dispersion and elite density" since S92, and S92 moved only the elite arm onto that population; the dispersion arm beside it kept reading the blended active-roster-plus-practice-squad figure, and the two reversed the verdict — blended sd 4.318 → 6.048 (drift 0.173/season, out-of-range) against active-roster 4.318 → 4.690 (drift 0.037, on-target). That is why S101 found this arm unassertable; it is asserted now, with the blended reading published beside it as an ungated contrast. S101's inherited diagnosis for the item — the generator's SUPERSTAR band — was disproved by measurement and not shipped.

`developmentDelta`'s trait centre was a literal for the third time (70, then 80, against an active roster whose mean potential reaches 83.57 by season 10) and is now measured from the live league. `derivedRng` reduced range with `hash % n`, keeping FNV-1a's weakest bits: 13/32 distinct pairs across sibling keys where independence predicts ~30, fixed to 31/32 with high-bit reduction plus a `fmix32` finalizer. Coaches have names. The bye week has its own beat. The initial shell returned under target at 642,587/650,000 bytes with zero lazy leaks by making the trade-deadline frenzy board a declared lazy root — its use was already gated to the declared window, only its download was not; the budget was not raised.

Two full-suite runs were discarded rather than accepted. The first certified a tree carrying an inconsistency this session introduced: `capCompliance.currentYearProration` read `contract.capYears`, a field that never existed on a persisted contract, so it fell through to `yearsRemaining` — which agreed with the old cap hit only because the cap hit was wrong the same way. The second surfaced a latent S101 defect: `rival-trade-offers`' fixture walked up to 14 weeks for any pending offer and handed it to a test that commits a trade, but S101's Week 11 deadline means a late offer can never be accepted. The engine was correct both times; the fixture and the second authority were the defects, and both were fixed at source with negative controls rather than worked around.

CDR reviewed: no new founder creative direction this session, so no entry is owed. Shell accounting: 1 started, 1 closed, 0 running.

## Session 103 — 2026-09-10 — The denominator answered; the re-point and the ratchet both refused on measurement

Recovered S102 first. Its engine work was committed and both staging and production were verified at `30572068`, but the closeout-artifact commit never landed: receipts, captures, the rendered brief and the status board sat uncommitted beneath a clean-looking history. The per-surface ledger showed all eight core surfaces already written in S102's own feature commit, so recovery was one commit (`5cde423`) rather than a re-implementation. The uncommitted `session102-lazy-frenzy-board` test was real work, passed 4/4, and was registered in the runtime shard on the way through.

S102's deferred denominator question had a factual answer. `LEAGUE_PROGRESSION_PARITY_TARGET` measures `rostered`, and a generated league starts with an **empty** practice squad and fills 16 slots per club over the following decade — so the gate's denominator gains about a quarter of itself, at a mean roughly ten points below the roster it is averaged into, across exactly the window it measures. A drift statistic needs a population that exists at both ends of its window, and that one does not. Shift-share on the canonical seed 20260306 over ten seasons: within-group **+0.269/season** (`watch`) against between-group **−0.197/season** (practice weight 0% to 22.7%), summing to the **+0.072** the gate asserts as `on-target`. S91 found this shape with the free-agent pool in this same denominator, S102 found it on the dispersion arm, and this is the third.

**The re-point was implemented and then reverted, and that is the decision of the session.** On the canonical seed `activeRosterOnly` reads **0.303/season** — `out-of-range` against `watchMaxAbs` 0.3, not `watch`. A red gate is closed by fixing the defect it found or by weakening the gate, and weakening is forbidden here, so the population waits on the fix rather than being paid for with a threshold. What ships instead is the finding as a machine-checked fact: `buildCompositionShift` decomposes the gate's own blended mean every run, and the realism regression now asserts it reports `verdict-changed-by-composition` on the canonical path. **This gate can no longer report `on-target` without also reporting, in the same receipt, that its denominator moved underneath it** — and that cost nothing in thresholds. The guard's criterion is a changed verdict rather than a tuned magnitude, because its first draft used a magnitude comparison and returned "development-dominated" when run against the very measurement that motivated it: 0.197 is not larger than 0.269, so it would have shipped green on its own defect.

The same receipt hands over the root-cause candidate for both the 0.303 reading and the standing elite-density `watch`. Active-roster **position composition** is drifting hard: `normalizeRosterSlots` ranks a club's whole roster by overall with no positional structure, so over ten seasons the quarterback room goes 64 to 195 and specialists 64 to 155 while the offensive line falls 288 to 258 and the front seven 480 to 407 — and the quarterback room carries 11.3% of its players at 90+. High-rated, cheap positions crowd out linemen, inflating the active-roster mean for a compositional reason one level below the practice-squad one.

S102's recommended fix for elite density was built and refused on measurement. Age-indexed potential decline (onset 27, 0.35/year past prime, capped at 3.0, RNG-free, applied at the reversion seam) ran against a matched control for ten seasons on seed 2026. The cost lands — active-roster mean potential 83.78 to 82.91 — and lands on the wrong thing: elite density did not move (3.80% to 3.92%) and the parity reading got worse, +0.265 to +0.298/season. The age mix says why: the active roster's mean age falls 27.38 to 26.0 and the elite cohort averages 26.6, so the rule reaches about a third of its target and replaces what it reaches with younger, higher-potential intake. Reverted whole, with no dead module left behind. Third consecutive session in which running the engine contradicted a plausible inherited diagnosis; the new rule is that a handoff's recommended *fix* is re-derived too, not only its diagnosis.

A league also had no identity. `staffSeedKey` and `coachingMarket.leagueSeed` both fell back to a key built from the league's start year whenever a league carried no `leagueId`/`franchiseId` — which is every single-player league, since only the multiplayer lobby sets those. `derivedRng` was working perfectly and being handed the same seed by every franchise, so head coaches, coordinators and coaching-market candidate pools were byte-identical across seeds 8121 / 2026 / 4242. `leagueIdentity` derives a per-league value from persisted content and the normalizer assigns it once. It fingerprints player ids rather than team identities because `buildRandomizedTeamIdentities` takes only the year — a team-derived fingerprint would have reproduced the exact bug it fixes. Two claims attached to this item were checked rather than inherited: the "regenerates staff for every existing save" migration cost is false (the builder preserves stored names and attributes, asserted on a real snapshot stripped to look pre-S103), and the third fallback site, `gmDecisionAuthority`, is not in the defect class at all — it keys a ledger held inside one save's own state, so it was left alone and said so.

CDR reviewed: no new founder creative direction this session, so no entry is owed.

**Post-commit — the browser gate, classified rather than re-run.** The first push turned CI's `browser-gates` red on exactly one Playwright spec while the other 59 passed and all 1,428 Node tests were green. The red was intermittent (2 failures in 10 local repeats) and the first hypothesis — that this session's league-identity change caused it — was wrong: a control that passed 10/10 with the change disabled against 8/10 with it is not evidence at that sample size, and chasing it cost real time. What settled it was the product's own weekly-plan receipt sitting inside the captured failure artifact: *"Bye week committed · GM choice · bye week — no opponent · gm-decision → bye"*. S102 made a bye suppress the tactic step entirely, and `s63-surfaces` still handled a bye **inside** the pre-game modal — a branch a bye now never opens. The browser league is generated from a random seed, so the failure rate is just how often the controlled team draws a bye in the first advanced week. The test now asserts against whichever authority the product actually produced (brief or bye receipt): 20/20 repeats, with both bye-branch strings verified directly against `describeWeeklyPlanReceipt`. Read the artifact the failure already wrote before building a control around a guess. Investigating it also turned up an independent defect: `createLeagueBase` returns zero players, so a league normalized before its roster is allocated fingerprinted an empty sample and produced one constant identity for every league — this session's own fix, rebuilt inside itself. `ensureLeagueIdentity` now refuses to persist below a fingerprintable floor.

## 2026-09-10 — Session 104 — Full arc: the league was never generated at the roster its own rules declare

**Recovered mid-implement, not started clean.** The tree held ~1,300 uncommitted lines across ten source files, an untracked `test/session104-fieldable-depth.test.js`, S103's capture-ledger entries still unstaged, and no session lock. F7 read "session likely in flight" and the per-surface ledger showed all eight core surfaces current through S103 — a cut-off during implementation, not a skipped closeout — so the recovery was to verify and finish the work rather than reconstruct a record.

**The defect three sessions had been circling was one level below all of them.** S102 published a divergence between two candidate gated populations and refused to choose. S103 proved `rostered` failed a prior test — a drift statistic requires a population that exists at both ends of its window — then measured the re-point at `+0.303/season`, `out-of-range`, and refused to buy it with a threshold change. **What no session tested is that `activeRosterOnly` fails the same test, mirrored:** if a club holds 49 players at the start of the window and 69 at the end, the active roster is an unfiltered 49 at one end and the top 53 of 69 at the other, silently acquiring a selection filter it did not have. Neither candidate was a valid drift statistic, so choosing between them could not have produced one. `ROSTER_TEMPLATE` summed to **49** against a declared `activeLimit` of 53, and generation built **no practice squad at all** against a declared `practiceLimit` of 16 — every league in this project's history began 20 players a club below its own steady state.

Generating at the declared structure, and reading that structure as a **floor** in `teamNeeds` via `FULL_ROSTER_TEMPLATE` rather than only as a ceiling in the compliance pass, moves the canonical ten-season decomposition on seed 20260306 from `1568→2178` rostered / practice weight `0%→22.7%` / between-group `−0.197` / within-group `+0.269 watch` to `2208→2183` / `23.2%→22.7%` / `+0.003` / `+0.040 on-target`. `activeRosterOnly` goes `+0.303 out-of-range` → `+0.107 on-target`. S103's composition guard now reports `development-dominated`, with its negative controls still fixture-driven so the detector is proved to fire against the pre-S104 shape.

**Three defects of the same family fell out of it.** The active 53 was a leaderboard rather than a depth chart — `normalizeRosterSlots` ranked by an `overall` that is not comparable across positions, so quarterback rooms went 64 → 195 and specialists 64 → 155 while the line fell 288 → 258 and the front seven 480 → 407, with 11.3% of quarterbacks at 90+. `FIELDABLE_DEPTH` now declares a `min` **and** a `max` per room and `assignFieldableActiveRoster` allocates in three passes — minimums, merit bounded by each ceiling, then legality. A minimum alone would not have fixed it: nothing in a minimum stops a sixth quarterback out-rating a fourth cornerback for the slot, which is exactly how the room grew. Both **release paths** were position-blind, and value density (overall per dollar) is largest for a cheap high-rated room and smallest for a starting lineman, so an unprotected cap cut stripped the line every time; `selectReleasable` guards a **count**, evaluated as cuts accumulate. And **draft intake was uniform** — `rng.pick` over nine positions gave every room 11.1% of every class forever, against demand ranging 1.9% to 18.9% — which does not equilibrate but accumulates, and the measured ordering is exact (QB +230%, K/P +169%, OL +9%, front seven +16%). `DRAFT_POSITION_WEIGHTS` derives from the declared template and draws with the RNG's existing `weightedPick`, so one value is still drawn per prospect; punters are drafted at last.

**Two of the three drafts of this session's own `teamNeeds` floor were reasoned and wrong, and only measurement caught them.** Reading the *generation* template against the *active* roster reported a permanent shortfall in whichever room lost the merit pass and sent the free-agency backstop shopping every offseason. Reading `FIELDABLE_DEPTH[pos].min` was worse: the minimums sum to 41, clubs restocked to 41, the league drained **2,208 → 1,905** over ten seasons and the practice squad fell from 16 a club to 6.5 — **the S103 moving-denominator defect with its sign flipped, installed by the fix for it.** The fieldability guard was wrong on its first draft too, in a way a fixture would not have shown: protecting each club's best `min` players *as individuals* is a no-trade clause, and on the S91 camp-cuts fixture it left a club $129.0M over the cap bottomed out at the 53-man floor still **$27.8M over, reporting itself trapped**, while holding eleven linemen it was never allowed to touch.

**The generated league had to stay affordable, and the curve was not shaved to make it fit.** Filling the declared roster on ordinary market contracts put **all 32 clubs over a $255M cap before a snap was played** — median space −$50.1M, worst −$84.7M — against a baseline of zero over-cap clubs and $43.4M median headroom. The compliance pass would have laundered it by cutting twenty players a club in the first offseason, so nothing would have gone red; the league would simply have started as fiction. A real roster pays its starters and key backups and fills the rest at the minimum, and `FIELDABLE_DEPTH[pos].min` is already this project's declaration of who those players are. Cap legality at generation is asserted across four seeds.

**A gate that could not fail, and behind it a module that could not load.** `scripts/lib/brief-preflight.mjs` has spawned `scripts/check-audit-premises.mjs` inside `try { … } catch { /* non-fatal */ }` since the premise-decay step was propagated here, and **the script was not in the repository**. No `.cache/audit-premise-decay.json` was ever written, the brief's SIGNALS row never appeared, and every typed `premises` block in `docs/AUDIT_*.json` had gone unchecked for the project's whole history — with nothing to say so. The checker reuses `isAuditComplete` rather than declaring a fourth copy of the completion vocabulary, treats an unobservable premise as a third outcome so a renamed target can never read as "the defect is fixed", and is advisory by default with `--strict` for a caller that wants a gate. Across the entire audit history: zero unverified, zero open decay — a **first** measurement, not a restored one.

Trying to prove that end to end exposed the second half. The same preflight *statically* imports `../check-last-session-summary.mjs`, which was **also absent**, so the module threw `ERR_MODULE_NOT_FOUND` on import and the spawn had never executed at all; nothing in this repo imports the preflight, so nothing ever raised it. That module's symptom had been visible the whole time and read as ordinary staleness: `brief-blocks.mjs` renders a `STALE LAST SESSION SUMMARY` block, `PROJECT_STATUS.lastSessionSummary` has described **S100 while four further sessions shipped**, and `doctor-remedies.mjs` names the missing writer as that check's remedy beside a comment stating the defect exactly — *"lastSessionSummary had a detector but no writer, so it silently went stale."* **I wrote that writer, and the studio shard was right to reject it.** `test/project-authority-contract.test.js` failed on the spot: the path is named in `context/PROJECT_AUTHORITY_CONTRACT.json` under `forbiddenFiles` — *"fail closed when generic Studio propagation erases project-specific authority contracts"* — and this project declares exactly three permitted `statusMutators`, so the module would have been a fourth mutator of `PROJECT_STATUS.json`. **Deleted rather than allowlisted.** The accurate finding is therefore not "a dependency is missing" but *"`brief-preflight.mjs` is generic machinery depending on a file this project deliberately forbids, and is inert here by design"*. What actually closes the four-session staleness is this project's own authority — `lastSessionSummary` written at closeout, by hand. The check that had been red for four sessions is green. A second claim made mid-session, that closeout must also bump `currentSession` to reconcile the board and the brief, was **wrong and is corrected in DECISIONS 11**: that field is derived by `resolveSessionAuthority` from the monotonic maximum of committed authorities and self-healed by the brief, so the hand bump became another authority and pushed the brief to *Session 106*. Reset to 104 it renders `Session 105`, `divergent=false`, no stale block.

**The general form shipped too, with the true invariant rather than the convenient one.** The naive rule — every static relative import under `scripts/` must resolve — would have *required* creating the forbidden file to go green, which makes it a gate satisfiable only by violating another gate. What ships adds the clause that makes it true: imports resolve **unless** the target is contractually forbidden, in which case **the importing module must have no importer**, so the forbidden dependency can never be exercised and wiring `brief-preflight` up trips a test instead of failing at runtime. Comments are stripped before matching, because docstring usage examples name modules relative to the *caller*: 37 hits before stripping, one real one after. Dynamic and computed specifiers are deliberately out of scope rather than approximated.

**The public status page owed players two sessions of release notes.** `check-public-truth`'s freshness gate counts sessions rather than days and tolerates exactly one, because the note lands at closeout — so one behind is in flight and two behind is a page that stopped tracking the product. It was at two: S103 shipped without a note and sat *exactly* on the tolerance, invisible until S104 pushed it over. `public/status.html` now carries a real player-facing note for what a player will actually notice — clubs dressing a legal depth chart rather than six quarterbacks and four kickers, new leagues starting at a full roster with a practice squad, the draft supplying the positions clubs need, and the cap binding from the first snap.

**Published rather than absorbed.** The distribution gate's dispersion arm moved `on-target` → `watch` (+0.095/season canonical, +0.105 and +0.111 on two probed seeds, against `stdDevDriftOnTargetMaxAbs` 0.08). It is the same artefact seen from the other side — the old `on-target` was produced by the active roster's selection filter *strengthening* across the window, and with the filter constant the arm reports the S91 random walk it was built to find. No threshold was moved in any gate this session, and the parity population is deliberately still `rostered`: both candidates are finally valid, and `rostered` reads +0.038 to +0.063 across seeds against an `onTargetMaxAbs` of 0.15 where `activeRosterOnly` reads +0.102 to +0.115.

CDR reviewed: no new founder creative direction this session, so no entry is owed.

**Canonical Node receipt: 1,446/1,446 across six shards** — core 245, runtime 817, sim-contract 83, sim-realism 1, long 5, studio 295 (up from 1,428/1,428 in S103, +18 tests).

The studio shard went **red on its first pass with three self-caused failures**, all fixed at source and the shard re-run green. None was flaky, sibling drift, or force-greened:

1. `project-authority-contract` — a new `scripts/check-last-session-summary.mjs` violated `forbiddenFiles`. Deleted, not allowlisted.
2. and 3. `public-compliance` and `session94-public-surface` — the same release-note freshness defect, seen from two gates: `public/status.html` was two sessions behind the product.

The core, runtime, sim-contract, sim-realism and long shards are from the single full-suite run; the studio shard is a re-run, which is required in any case because this session added a test file to it after that run began.

Other gates, all green at the final tree: `check-public-truth` (42 engine systems, 102 shipped surfaces) · `check-windows-hide` (which caught a real red on this session's own new test and was fixed at the call site) · browser boot budget 642,587/730,000 with zero lazy leaks · browser module reachability 92 modules from 4 HTML entries · `validate-brief-format` conformant · `check-project-authority-contract` ok · `check-audit-premises` zero unverified and zero open decay across all 83 committed sidecars.

## 2026-09-11 — Session 105 — Full arc: every surface the brief reads gets a writer and a test that fails when it is skipped

**Triage.** Clean start. F7 reported write-back debt on `ccf68c2`; that is S104's own post-closeout handoff edit (the known closeout-order false positive), and every core surface was current through S104. No lock, clean tree, synced with origin. Profile: app · product rubric · direct-to-main with staging first · public → sanitize (studio-ops profiler, `--project .`; no local copy exists, labelled accordingly).

**Audit** (`docs/AUDIT_2026-09-11_SESSION105.json`, six items, combined priority 111, every premise verified against live code before inclusion) came from reading every derived row of the rendered S105 brief rather than the rows that looked wrong.

**Shipped:**

1. `parseHandoffCloseoutAuthority` reads `# Latest Handoff — Session N → Session M` (left-hand session only); `brief-semantic-fingerprint` routes through it. None of twelve committed handoffs had ever matched. The live handoff is bound to the newest SIL entry by test, with a negative control reproducing the pre-S105 pattern.
2. `scripts/render-sil-rolling-status.mjs` derives the SIL rolling-status block from the live file and its archive; `test/session105-brief-authority-writers.test.js` fails when the committed block lags a fresh derivation. Derived Avg3 959.7 against the hand-written 974.7; 58 unique `/1000` sessions against a claimed 87.
3. The same test walks every `docs/AUDIT_*.json` through `checkAudit` and fails on open decay or unverified premises, with a fixture negative control. First run: red on this session's own sidecar (shipped items still `planned`); resolved by recording evidence.
4. Elite density re-derived under the S104 roster shape (canonical seed, ten seasons): active-roster 90+ share 0.4% → 2.8%, `watch`; dispersion 0.095/season and parity +0.043 reproduce S104 to the third decimal. A probe reproducing the gate's clone path (2.79% against the gated 2.8%) decomposes the cohort by room: QB 11 of 95 (11.6%), OL 19 of 311 (6.1%), every other room 17 of 1,279 — QB and OL are 24% of the roster and 64% of its elite. Those are the only two rooms a generated league places above their own mean potential (OL 82.1 vs 79.8, QB 82.7 vs 80.9). Recorded as the measured candidate; no statistic or generator change shipped on one seed.
5. `brief-preflight` importer question closed as DECISIONS S105-4.
6. PROJECT PROFILE row shipped to studio-ops as Ark `pattern-share` cargo `01K291HHVU080E58C015E99770`.

**Two of my own first drafts were wrong and tests caught both:** the intent rate divided by all five entries (an entry with no recorded outcome counted as a miss), and the sparkline rescaled min-to-max within five scores. Code fixed, fixtures untouched.

CDR reviewed: no new founder creative direction this session, so no entry is owed. No player-facing change, so no status-page release note is owed (the freshness gate tolerates one session).

**Deploy.** `e541418` pushed to main, staging verified **14/14** at that revision with rollback available, and the production promotion then **failed in its `build` job** on an intermittent browser test — `deploy` skipped, production not republished. The same `play-mode-smoke` assertion failed in S104's CI run on `ccf68c2` and passed in S105's CI run on the very commit the promotion failed, so it is intermittent rather than S105 breakage (nothing under `src/` or `public/` changed). Cause: S102 made a bye suppress the tactic step, S103 taught `s63-surfaces` that, and `play-mode-smoke` still asserted the tactic modal unconditionally against a randomly seeded browser league (~1 bye in 5). Fixed at source rather than re-run for a green; the bye branch's literals are pinned against `describeWeeklyPlanReceipt` in the S105 studio test, with a negative control. Local `npm run test:ui` — the promotion gate's own command — passes **60/60**. The fix was committed as `4d291ee`, deployed to staging (**14/14**, deployment `eacad999-2d78-4bef-9f3a-d048c98af0bb`, rollback available) and promoted cleanly — run **34655574507**, gate/build/deploy all success, live origin verified **10/10**. Release authority is **verified** with all four identities (staging · production · visual · performance) bound to `4d291eec`; `stagingAuthority` reconciled separately at the same revision; doctor `blockingFailing 0` (11/12, the standing registry-drift warning). `launchReady` stays **false** — technical deployment, not public launch.

**Receipts.** Canonical Node **1,458/1,458** across six shards (core 245, runtime 817, sim-contract 83, sim-realism 1, long 5, studio 307), up from 1,446/1,446 in S104. The full `npm test` read 1,450/1,452 — every behaviour shard green, two self-caused studio reds — and the studio shard was re-run green after both were fixed at source. Nothing under `src/` or `public/` changed after the full run.

**Both reds were in this session's own record-keeping, and the second one mattered.**

1. The new SIL rolling-status gate went red on **byte-identical text**: the file is CRLF in this working copy and LF once git normalises it, so a re-save after `--write` flipped the block's endings. A terminator-sensitive comparison is red locally and green in CI. Fixed at source (normalise line endings) with a CRLF regression test and a negative control.
2. `DECISIONS.md` hit the 96 KB ledger ceiling, and **running the gate's own remedy, `ledger-roll.mjs --apply`, archived this session's record.** The roller assumed every ledger is newest-first and kept the first ten matching entries; DECISIONS, TRUTH_AUDIT and SELF_IMPROVEMENT_LOOP are newest-last, so it kept the oldest and archived the newest — the live SIL was left at sessions 85–98 with zero intent lines. Compounding it, the DECISIONS/TRUTH_AUDIT patterns matched only the pre-S100 `— Session N` heading, so the cut landed at the eleventh old-style entry and every modern section below it moved as one block. Reversed exactly (current archive minus committed archive, asserted; no `git checkout` on the live ledgers, which would have discarded this session's uncommitted work), then fixed at source: per-ledger `order`, suffix retention for newest-last, header kept live, archived block appended at the correct end, pointer kept at EOF, patterns matching both heading eras. Three tests plus a negative control reproducing the inversion, and a direct invariant — every live ledger still holds this session's own entry after a roll. Re-rolled correctly: oldest 38/12/12 archived, 286 KB → 147 KB, S105 intact.

<!-- ledger-roll:pointer -->
---

Older entries are retained verbatim in `logs/archive/WORK_LOG.archive.md`. Nothing is summarised or removed on the way; the live file holds the working set only (newest 10 entries), so a reader does not pay for the whole project's history to learn what is true this week.

## 2026-09-12 — Session 106 — Full arc: potential stops being drawn on a scale overall does not share

**Triage.** Clean start: F7 write-back current, tree clean, brief fresh and coherent for S106, canon synced (54 canons, Ark drained).

**Audit** (`docs/AUDIT_2026-09-12_SESSION106.json`, 4 items, combined priority 82) opened on the S105 handoff's named candidate and found the generator defect beneath it. Every premise verified against live code first — and the premise checker caught **two of my own typed premises** matching explanatory comments rather than the code they described, which is the gate working on its author again.

**Shipped: three of four items.** The headline generator change was implemented, measured twice and **reverted** — `session90-development-environment` read 0.285 against a 0.25 tolerance and stayed red at 0.289 after per-room trait centres were also implemented and measured. Reversion's mean contribution measured exactly 0.000, so the cause is not a miscentred term: deltas are applied to ratings and overall is recomputed with position weights, so changing which players move moves aggregate OVR at zero mean delta. Closing the gate would have meant duplicating the engine in the test or widening its tolerance; both refused. `src/` carries a 37-line comment recording the measurement, verified by diff as zero behaviour change. **The next item is the test's declared curve, not the generator.**

**Measured, implemented, reverted:**

1. **Position-aware potential.** `randomPotential` drew one band for every position while `overall` is a position-weighted average of position-biased attributes. Measured at generation, four seeds, 8,832 players: mean potential ~80 everywhere against mean overall 73.6-81.7; **36.8% of the league generated above its own potential**; all 45 elite players in a fresh league were QB or OL. Potential is now trait-sized headroom above the player's own overall, tapered by age, damped near the ceiling, drawn in the same stream slot (one `rng.int`). Result: above-own-potential **36.8% → 0.0%**, mean potential tracks the room, **every mean overall unchanged to the decimal**.
2. **The counterweight discovery.** The uncalibrated fix read elite **5.3%** and dispersion **0.157**, both out-of-range: the old defect had been a downward brake in the reversion term. Ceiling-proximity damping restored it with no threshold moved — elite **2.8% → 1.8%**, dispersion **0.095 → 0.083**, parity **+0.043 → +0.034**.
**Shipped:**

3. **Ledger-budget gate population.** WORK_LOG (213 KB) and TASK_BOARD (206 KB) were unpoliced; both registered with directory, order and both heading eras, then rolled (**572 KB → 244 KB**, S105 entries and `Now`/`Next` intact). The population is now derived from disk by test.
4. **Brief reports post-heal authority**, and the doctor remedy naming a contractually forbidden writer is deleted with a test to keep it gone.

CDR reviewed: no new founder creative direction, so no entry is owed. No player-facing surface changed, so no status-page release note is owed (the freshness gate tolerates one session).

**Receipts.** Canonical Node **1,463/1,463** across six shards (core 245, runtime 817, sim-contract 83, sim-realism 1, long 5, studio 312), up from 1,458/1,458 in S105. The full run read 1,461/1,463 with two self-caused studio reds — one defect seen from two gates: `status.html`'s newest release note sat at session 104 against a `lastSession` of 106, two unpublished against a tolerance of one. Fixed at source by writing the note; studio re-run green. An earlier full run in this session, before the revert, read 1,450/1,452 and is superseded — it measured code no longer in the tree.

**The release note says nothing changed, because nothing did.** Neither S105 nor S106 shipped a player-facing surface, and the gate counts sessions rather than features, so the note says so plainly, records that the ceiling-scale repair was built, measured twice and withdrawn, and confirms saved franchises are untouched.
