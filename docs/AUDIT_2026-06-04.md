<!-- generated-by: /audit skill v1.0 -->
<!-- generated-at: 2026-06-04 -->
<!-- project: vaultspark-football-gm · type: game -->

# Project Audit - VaultSpark Football GM

> One ranked improvement plan covering 9 axes. Top items first.
> Run `/implement` to execute in optimal order.

## Top-line

- Total items: 8
- Combined Priority: 226.1
- Top 3: shareable-challenge-codes · season-epilogue-ritual · rivalry-heat-surfacing

## Audit context (Session 14 — personalized)

- **Live incident found during audit:** both ship pipelines (CI `browser-gates`, Pages deploy `build`) hung 25–30 min at `npx playwright install` and were timeout-cancelled on the Session-13 push — the shard matrix has never gone green in Actions and the published site is stale.
- **Live incident found during audit:** the custom-domain HTTPS cert (`vaultsparkstudios.com`) is `bad_authz`, expired 2026-06-02, with `https_enforced: true`.
- **Blocker reclassified:** "GitHub Pages provider/repo settings need external confirmation" — `gh` is authenticated with `repo`+`workflow` scopes, Pages is configured (`build_type: workflow`); remaining work is agent-attemptable cert remediation, not founder action.
- **Engine→UI gap:** `rivalryDNA.js` is fully built and invisible — zero game-UI references.
- **SIL follow-up debt:** the scheduled deep realism sweep is recorded twice in SIL follow-ups and never shipped.

## Ranked Plan

| # | Tier | Axis | Effort | Impact | Innov. | Priority | Item |
|---|:-:|---|---|:-:|:-:|:-:|---|
| 1 | 🔥 | gamification | 2h | 9 | 9 | 40.5 | **shareable-challenge-codes** - Zero-backend viral loop. The deterministic sim means a compact base36 code (seed + team + ruleset + target) lets a friend replay your exact challenge and beat your result — no accounts, no server, works on static Pages. Nobody in this category does deterministic challenge-code duels. **Recipe:** `public/lib/challengeCodes.js` encode/decode, Export button on the speedrun card, Enter-code input on setup that seeds the identical league with the rival's result as the target. |
| 2 | 🔥 | gamification | 2h | 9 | 7 | 31.5 | **season-epilogue-ritual** - End-of-season is the churn point and the payoff is scattered: arcs resolve silently, records land in a board, sentiment shifts without ceremony. One cinematic epilogue (arc verdicts → records → fan trajectory → closing press quote) converts churn into "one more season." All pieces exist; this is wiring, not systems. **Recipe:** `buildSeasonEpilogue()` aggregator + multi-card modal reusing Franchise Moment styling + share hook into leagueStoryExport. |
| 3 | 🔥 | gamification | 1h | 8 | 6 | 30.4 | **rivalry-heat-surfacing** - `rivalryDNA.js` tracks heat/streak/history for every matchup; grep proves zero game-UI usage. Shipped depth generating no felt experience. **Recipe:** expose rivalry context via local API runtime, heat badge on the upcoming-game card in tabOverview, RIVALRY WEEK banner in sim-watch header above a heat threshold. |
| 4 | 🔥 | feedback loop | 1h | 8 | 6 | 30.4 | **beta-feedback-widget** - Beta is the milestone and there is no in-game way to report anything; analytics signals never leave the browser. "Tell the Commissioner" closes the loop on a static host with no backend. **Recipe:** Settings-tab button + post-Franchise-Moment prompt opening a prefilled GitHub issue URL with game context; opt-in analytics digest via clipboard. |
| 5 | 🔥 | speed/organization/efficiency | 1h | 10 | 3 | 19.0 | **ci-playwright-hang-fix** - Step logs prove `playwright install --with-deps chromium` downloaded to 100% then hung 25 min in BOTH ship pipelines. Every deploy since Session 13 has died here. **Recipe:** actions/cache on `~/.cache/ms-playwright` keyed to Playwright version, `timeout-minutes: 6` + one retry on the install step, `DEBIAN_FRONTEND=noninteractive`, global watchdog timeout in `smoke-pages.mjs`. |
| 6 | ⚡ | security | 1h | 7 | 5 | 22.1 | **save-integrity-guard** - Beta franchises live in localStorage + optional gist sync; one corrupt write silently kills a 40-season dynasty and that tester never returns. **Recipe:** FNV-1a checksum + schemaVersion stamp on browser save envelopes, verify-on-load with "restored from last good slot" toast, stamp gistSync payloads identically. |
| 7 | ⚡ | security | 30m | 9 | 3 | 20.5 | **pages-cert-remediation** - Cert is `bad_authz`, expired 2026-06-02, `https_enforced: true` — the launch domain is broken right now. Agent-attemptable: `gh` has repo scope. **Recipe:** re-set cname via `gh api` PUT to retrigger ACME, verify DNS A/AAAA targets, poll cert state, escalate with exact DNS instructions only if ACME fails again. |
| 8 | ⚡ | speed/organization/efficiency | 1h | 7 | 4 | 17.7 | **realism-sweep-cron** - The same follow-up appears in two consecutive SIL entries and was never shipped: deep realism confidence currently depends on someone remembering to run `test:long`. **Recipe:** `.github/workflows/realism-sweep.yml` weekly cron running `test:long` + `verify:realism` with its own timeout budget and uploaded summary artifact; never push-blocking. |

## Innovation reserve (Innovation ≥ 8 — execute these even when busy)

- 1 item: shareable-challenge-codes (9)

## Skipped (deferred, not culled — all candidates scored above 1.5)

- press-conference-narrative-crossrefs — strong AI-axis idea (press quotes referencing live rivalry/arc context); deferred to keep the sprint one-session shippable; natural extension of item 3.
- fan-sentiment-hot-seat-loop — sentiment is partially surfaced already; the consequence loop (owner patience / hot seat) deserves its own design pass.

## Execution Log

- 2026-06-04 - `ci-playwright-hang-fix` shipped. Both workflows now cache `~/.cache/ms-playwright` keyed to the lockfile, run the Playwright install with a 6-minute step timeout plus one retry, set `DEBIAN_FRONTEND=noninteractive`, and bound the smoke/UI steps; `scripts/smoke-pages.mjs` gained a 180s force-fail watchdog. Evidence: `npm run build:pages` + `npm run smoke:pages` pass locally; next push exercises the CI defense.
- 2026-06-04 - `realism-sweep-cron` shipped. `.github/workflows/realism-sweep.yml` runs weekly (Mon 06:17 UTC) + on dispatch: `test:long` then `verifyRealism --seasons 24`, 90-minute budget, report uploaded as artifact, never push-blocking. Closes the twice-recorded SIL follow-up.
- 2026-06-04 - `pages-cert-remediation` resolved as diagnosis + escalation (agent attempt completed per blocker preflight). Findings: cert lives on the org root repo `VaultSparkStudios.github.io` (cname `vaultsparkstudios.com`), state `bad_authz`, expired 2026-06-02; DNS apex points at Cloudflare proxy IPs so GitHub's HTTP-01 ACME can never complete; visitor TLS works via Cloudflare edge cert, but the site serves a Cloudflare-origin 403 on all paths — the launch domain is down. Same-value cname PUT executed (no-op as expected); Cloudflare capability MISSING from secrets gateway; root-domain delete/re-add not taken unilaterally. Founder runbook written to TASK_BOARD.
- 2026-06-04 - `rivalry-heat-surfacing` shipped. `#rivalryStrip` renders heat label/meter/series/streak for the controlled team's matchup above the schedule table (tabOverview), and sim-watch headers gain a pulsing RIVALRY WEEK banner at heat ≥ 60 via `/api/rivalry`. Evidence: runtime shard 69/69; Pages smoke pass.
- 2026-06-04 - `season-epilogue-ritual` shipped. New `public/lib/seasonEpilogue.js` aggregates arc verdicts, records set in the closing season, fan-approval meter, and a deterministic outcome-keyed coach quote into the existing Season Review modal (gameFlow hook). Evidence: runtime shard green; epilogue degrades gracefully when endpoints return nothing.
- 2026-06-04 - `shareable-challenge-codes` shipped. New `public/lib/challengeCodes.js` (VSFC1 base64url payload + FNV-1a checksum), `leagueMeta` added to `/api/speedrun/status`, Copy Challenge Code button on the speedrun panel, Accept Challenge input on setup that prefills seed/year/team and stores the rival target. Evidence: 8/8 unit tests (round-trip, tamper rejection, normalization).
- 2026-06-04 - `save-integrity-guard` shipped. FNV-1a stamp + verify in `saveStoreShared.js`, stamped on browser save meta and verified on load (legacy saves still load), gist sync gains an integrity sidecar file verified on import. Evidence: 6/6 new tests including the valid-JSON-but-corrupt case; quota test budget adjusted +200 bytes for the stamp.
- 2026-06-04 - `beta-feedback-widget` shipped. New `public/lib/betaFeedback.js` — "Tell the Commissioner" panel in Settings + link on the Franchise Moment card; prefilled GitHub issue URL with game context, opt-in analytics digest via clipboard. Evidence: 4/4 URL-builder tests.

**Sprint verification:** all five default shards green — core 54, runtime 69, sim-contract 22, sim-realism 1, studio 3 (149 total, up from 131) · `build:pages` + `smoke:pages` pass.

---
*Generated by `/audit` v1.0 · ready for `/implement`*
