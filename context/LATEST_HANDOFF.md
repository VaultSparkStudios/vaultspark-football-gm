# Latest Handoff — Session 104 → Session 105

## Where We Left Off

S104 was recovered, not started clean. The prior S104 session had committed nothing: a dirty tree of ~1,300 inserted lines across ten source files, an untracked `test/session104-fieldable-depth.test.js`, S103's capture-ledger entries still uncommitted, and no session lock. F7 write-back currency read "session likely in flight", and the per-surface ledger showed all eight core surfaces current through S103 — so this was a **mid-implement** cut-off, not a skipped closeout, and the recovery was to verify and finish the work rather than reconstruct a record.

**The defect three sessions had been circling was one level below all of them.** S102 published a divergence between two candidate gated populations and refused to choose. S103 answered the choice — `rostered` fails a prior test, because a drift statistic requires a population that exists at both ends of its window and a generated league's did not — then measured the re-point at `+0.303/season`, `out-of-range`, and correctly refused to buy it with a threshold change.

What no session tested is that **`activeRosterOnly` fails the same prior test, mirrored.** If a club holds 49 players at the start of the window and 69 at the end, then the active roster is an *unfiltered 49* at one end and the *top 53 of 69* at the other: it silently acquires a selection filter it did not have. That is why it read 0.303. **Neither candidate was a valid drift statistic, and choosing between them could not have produced one.**

The cause was never the choice of population. `ROSTER_TEMPLATE` summed to **49** against a declared `activeLimit` of 53, and generation built **no practice squad at all** against a declared `practiceLimit` of 16. Every league in this project's history began 20 players a club below its own steady state.

## What shipped

Measured on the canonical seed 20260306 over ten seasons — the path `realism-career-regression` asserts on:

```
                         before S104            after S104
rostered population      1568 -> 2178           2208 -> 2183
practice-squad weight    0%   -> 22.7%          23.2% -> 22.7%
within-group drift       +0.269  watch          +0.040  on-target
between-group drift      -0.197                 +0.003
blended (gated)          +0.072  on-target      +0.044  on-target
activeRosterOnly         +0.303  out-of-range   +0.107  on-target
```

- **Generate at the declared structure.** `ROSTER_TEMPLATE` now sums to `activeLimit` and `PRACTICE_SQUAD_TEMPLATE` to `practiceLimit`, bound to `ROSTER_STRUCTURE` by test so neither constant can drift out of the other's reach — the failure mode `CONTRACT_RULES.maxSalary` shipped for years.
- **Read the same structure from both sides.** `FULL_ROSTER_TEMPLATE` is a **floor** in `teamNeeds`, not only a ceiling in the compliance pass. This was the second-order defect, and the second version of it: pointing `teamNeeds` at `FIELDABLE_DEPTH[pos].min` was tried first and measured — the minimums sum to 41, clubs restocked to 41, the league drained **2,208 → 1,905** over ten seasons and the practice squad fell from 16 a club to 6.5. **That is the S103 moving-denominator defect with its sign flipped, installed by the fix for it.**
- **A fieldable depth chart, not a leaderboard.** `FIELDABLE_DEPTH` declares a `min` *and* a `max` per room; `assignFieldableActiveRoster` allocates in three passes — minimums, merit bounded by each ceiling, legality — and is exported pure so the property is assertable on a roster without simulating a decade.
- **Both release paths guarded by a count.** `selectReleasable` refuses only the move that would drop a room below its minimum. Who fills that minimum stays a personnel decision the club is entitled to get wrong.
- **Draft intake proportional to demand.** `DRAFT_POSITION_WEIGHTS` is derived from the declared template and drawn with the RNG's existing `weightedPick`, so exactly one value is drawn per prospect as before. Punters are drafted.
- **Role-priced generation** plus a bounded generation-time affordability pass, asserted across four seeds.
- **Two propagated modules were referenced and absent, and each hid differently.** `scripts/check-audit-premises.mjs` was *spawned* by `brief-preflight` inside a silent `catch`, so its absence produced no cache, no SIGNALS row and no error — and **every typed `premises` block in `docs/AUDIT_*.json` went unchecked for the project's whole history.** `scripts/check-last-session-summary.mjs` was *statically imported* by that same preflight, which made the whole module un-importable; nothing in this repo imports the preflight, so the `ERR_MODULE_NOT_FOUND` was never seen. Its live symptom was visible all along: the brief has rendered `STALE LAST SESSION SUMMARY` and replayed **S100** for four sessions, and `doctor-remedies` names the missing writer as that check's remedy.
- **The second one was written, and then removed — read this before you write it again.** `context/PROJECT_AUTHORITY_CONTRACT.json` names `scripts/check-last-session-summary.mjs` in `forbiddenFiles`, whose declared purpose is to *"fail closed when generic Studio propagation erases project-specific authority contracts"*. This project declares exactly three permitted `statusMutators` and a generic writer would be a fourth mutator of `PROJECT_STATUS.json`. `test/project-authority-contract.test.js` caught it on the studio shard, and it was deleted rather than allowlisted. **This project's authority for `lastSessionSummary` is the closeout write, by hand** — and that write, on its own, is what closed the four-session staleness. `scripts/lib/brief-preflight.mjs` is therefore un-importable here *by design*, not by accident: it is generic machinery that depends on a file this project forbids.
- **And the general form shipped as a gate — with the true invariant, not the convenient one.** The naive rule ("every static relative import must resolve") would have demanded creating the forbidden file. What ships: every static relative import under `scripts/` resolves **unless** its target is contractually forbidden, in which case **the importing module must have no importer**, so the forbidden dependency can never be exercised. Wiring `brief-preflight` into anything now trips `test/session104-audit-premise-decay.test.js` rather than failing at runtime.
- **`public/status.html` owed players two sessions of release notes.** The freshness gate counts sessions and tolerates one, because the note lands at closeout — S103 shipped without a note and sat *exactly* on the tolerance, so the debt was invisible until S104 pushed it to two. A real player-facing note now covers what a player will notice: clubs dressing a legal depth chart instead of six quarterbacks and four kickers, new leagues starting full, the draft supplying what clubs need, and the cap binding from the first snap.

## Five things that were wrong before they were right — the class matters more than the instance

1. **A minimum alone does not fix a composition drift.** Nothing in a minimum stops a sixth quarterback out-rating a fourth cornerback and taking the slot on merit, which is exactly how the room grew. The `max` is the term that binds. If you find yourself declaring a floor to fix an accumulation, ask what stops the accumulation.
2. **A fieldability guard must protect a count, not people.** The first version made each club's best `min` players at every position unreleasable — a no-trade clause, not a roster rule. On the S91 camp-cuts fixture a club **$129.0M over the cap** bottomed out at the 53-man floor still **$27.8M over and reporting itself trapped**, while holding eleven linemen it was never allowed to touch. It was not trapped.
3. **A fix for a moving denominator can install a new one.** Both the `teamNeeds` corrections above are this. Measure the thing you just fixed *and the thing next to it*, over the same window, before believing the fix.
4. **A field with a derivation function is not a field you set.** I wrote down an invariant about who owns `currentSession` — and recorded it in five places — before checking the code that computes it. `resolveSessionAuthority` derives it from the monotonic maximum of committed authorities; my hand-set bump became one of those authorities and pushed the brief to *Session 106*. It took two minutes to disprove empirically and was only disproved because the fix was verified end to end rather than declared done. Before writing an invariant about a value, find what computes it; when the check is cheap, run it instead of reasoning.
5. **When a gate you did not write fails your change, the first hypothesis is that the gate is right.** Writing the missing `check-last-session-summary.mjs` looked obviously correct and was forbidden by contract. Read what a gate is protecting before touching it, and never allowlist your own change through one — and note the corollary that shaped the import gate: **a rule that can only be satisfied by violating another rule is not a gate, it is a trap.**

## Next work

- **Re-measure elite density against its standing `watch`.** S103 named active-roster position composition as the measured candidate for both the 0.303 parity reading and the elite-density watch. The composition driver is now removed and the parity reading is closed; the elite-density reading has **not** been re-derived under the new roster shape. Do that before proposing any cost — and note S102's recommended cost (age-indexed potential decline) was already built, measured against a matched control and reverted in S103, so do not re-implement an inherited diagnosis.
- **Do not close the dispersion arm by widening `stdDevDrift*`.** It moved `on-target` → `watch` this session (+0.095/season on the canonical path, +0.105 and +0.111 on two probed seeds, against `stdDevDriftOnTargetMaxAbs` 0.08). That is not a regression this session caused — it is the same artefact seen from the other side. The old `on-target` was produced by the active roster's selection filter **strengthening** across the window, which compresses measured dispersion exactly as the underlying spread widens. With the filter constant, the arm reports the S91 random walk it was built to find. It is a real reading of a real defect that now has nowhere to hide.
- **Do not re-point the parity population.** It is now a decision rather than an inheritance: both candidates are valid drift statistics at last, `rostered` reads +0.038 to +0.063 across seeds against an `onTargetMaxAbs` of 0.15, and `activeRosterOnly` reads +0.102 to +0.115. Moving a declared target for no measured reason trades a comfortable margin for a tight one. The contrast stays published, ungated, in `activeRosterMeanOverallDrift`.
- **Consider wiring `check-audit-premises.mjs --strict` into the studio shard or `doctor`.** It is advisory today because its caller is a brief render that must not be blocked. Nothing yet makes open premise decay *fail* anything; `test/session104-audit-premise-decay.test.js` proves the gate can fire, but only on a fixture.
- **`scripts/lib/brief-preflight.mjs` is importable again but still has no importer.** It loads and completes all three of its steps when called directly — this session called it, which is how the premise-decay cache came to exist for the first time — but `render-startup-brief.mjs` does not import it, so those steps only run when somebody invokes them by hand. It was deliberately **not** deleted: zero-importer files in this repo have repeatedly turned out to be unwired assets rather than dead code. Decide whether the brief renderer should call it, rather than leaving the question open a third time.
- **`currentSession` is derived, not set — and a wrong invariant about it nearly shipped.** `resolveSessionAuthority` computes the next session from the **monotonic maximum of every committed authority**, and `render-startup-brief.mjs` self-heals `status.currentSession` at session start. **Do not bump it at closeout.** S104 did, to 105, and the brief then rendered *Session 106* because the bump was read as another committed authority; reset to 104 — the session that just ran — the brief renders `Session 105` with `session-authority: committed=S104 · SIL=S104 · status=S104; divergent=false` and no stale block. An earlier version of this handoff claimed the board and the brief disagreed about the field's meaning and needed a closeout bump to reconcile; that was wrong and is corrected in DECISIONS 11. The real cause of the four-session staleness is the plain one: `lastSessionSummary` had a detector and no writer. **Write the summary at closeout and leave the counter alone.**
- **`doctor-remedies.mjs` names a `last-session-summary` check that `doctor.mjs` does not define here.** The writer it points at now exists and works, but no check fires it. Adding the check is a real item; one was not invented this session, because building against a contract nobody declared is how phantom work starts.
- The authoritative registry still reads `sparked` against a local contract of FORGE. An Ark `registry-delta` was shipped to studio-ops in S101; do **not** resolve this by flipping the local contract — three blocking lifecycle checks are written as `expected !== "FORGE" || …` and would become silent auto-passes.
- Delivered/reply-capable project-domain email, candidate-bound public-launch approval, and authoritative lifecycle reconciliation remain independent launch evidence. Public launch remains **HOLD**.

## The costs a 41% larger league actually carries

A club now holds 69 players instead of 49, so the league holds **2,208 active players instead of 1,568**. Two consequences were measured rather than assumed:

- **Save payload.** A freshly generated league's raw snapshot is **5.01 MB against `save-payload-budget`'s 16 MB guard**, and the end-to-end test — a full season of realistic play, backing up every week, inside a 5 MB browser origin — still passes. Snapshots are gzip+base64 in storage, so 5.01 MB raw is not the stored figure. There is real headroom, but it is roughly 40% less than it was; check this number before adding anything per-player to the snapshot.
- **Wall clock.** Every simulation test costs proportionally more. The `core` shard alone went from minutes to ~28 on this machine, and the whole canonical run took roughly 85 minutes against a 45-minute-per-shard timeout. No shard timed out, but `core` and `runtime` now have materially less margin than they did. If a shard starts reporting exit 124, that is the budget, not a hang — raise `TEST_SHARD_TIMEOUT_MS` deliberately and say so, rather than treating it as flake.

## One receipt is deliberately stale — read this before treating it as a defect

`.cache/test-count.json` still holds **1,415/1,415 generated 04:47**, which is the cut-off S104 session's run, not this one. The startup brief will therefore show `Tests … STALE — run`, and that is correct and honest rather than a bug.

Why it is stale: the shard runner writes a green receipt only when a full `all` run exits 0, and this session's canonical run had **three self-caused studio reds on its first pass**. Those were fixed at source and the studio shard re-run green (295/295), but a re-run of one shard does not produce a whole-suite receipt. A second full `all` run was started purely to refresh the cache and **deliberately stopped after a minute**: it costs ~85 minutes, `.cache/` is gitignored, the measured canonical figure is already recorded with full provenance in `PROJECT_STATUS.testShardReceipt`, and the brief's own STALE marker is the system working rather than failing.

There is no coverage gap behind it. The canonical **1,446/1,446** was measured against the source committed as `24ad30e`, and nothing under `src/` or `public/` changed after that commit — the two later commits are docs, context, reports and captures only. The individually affected studio tests were re-run after those edits (32/32).

**If you want the marker gone, run `npm test` once at the top of the session** and it will write a genuine receipt. Do not hand-write `.cache/test-count.json`.

## Verification boundaries

The parity decomposition, the roster-composition figures and the population counts are a **single-seed, ten-season** reading on the canonical seed 20260306, taken from `runRealismVerification({ seasons: 10 })` — the path `realism-career-regression` itself asserts on. The cross-seed statements (`+0.038 to +0.063`, `+0.102 to +0.115`, the dispersion arm's `+0.105`/`+0.111`) are two additional seeds, not a sweep. Generation-time cap legality is asserted across four seeds (20260306 / 2026 / 8121 / 4242); every other measurement in this handoff is not.

The figures quoted for the *pre*-S104 state are readings taken by the cut-off S104 session before its fix landed and are reproduced here from its in-tree source notes; they were not re-measured against a restored pre-fix tree during recovery. What **was** re-derived in recovery is the shipped state: the test suite asserts the post-fix decomposition, the fieldable bands, both release paths, the draft mix and cap legality directly, and every negative control in `session104-fieldable-depth.test.js` reproduces the pre-fix rule explicitly rather than relying on the canonical league to stay broken.

SIL scores are engineering assessments, not measured player outcomes. No real-cohort evidence exists, and none was manufactured.

## Receipts

**Canonical Node receipt: 1,446/1,446 across six shards** — core 245, runtime 817, sim-contract 83, sim-realism 1, long 5, studio 295 (up from 1,428/1,428 in S103, +18 tests).

The studio shard went **red on its first pass with three self-caused failures**, all fixed at source and the shard re-run green. None was flaky, sibling drift, or force-greened:

1. `project-authority-contract` — a new `scripts/check-last-session-summary.mjs` violated `forbiddenFiles`. Deleted, not allowlisted.
2. and 3. `public-compliance` and `session94-public-surface` — the same release-note freshness defect, seen from two gates: `public/status.html` was two sessions behind the product.

The core, runtime, sim-contract, sim-realism and long shards are from the single full-suite run; the studio shard is a re-run, which is required in any case because this session added a test file to it after that run began.

Other gates, all green at the final tree: `check-public-truth` (42 engine systems, 102 shipped surfaces) · `check-windows-hide` (which caught a real red on this session's own new test and was fixed at the call site) · browser boot budget 642,587/730,000 with zero lazy leaks · browser module reachability 92 modules from 4 HTML entries · `validate-brief-format` conformant · `check-project-authority-contract` ok · `check-audit-premises` zero unverified and zero open decay across all 83 committed sidecars.
