# Session 48 Implementation Plan

Source: `docs/AUDIT_2026-07-16_SESSION48.json`.

1. Simulation-truth foundation: `injury-authority-rehab-command` — consolidate the injury clock, then expose its honest tradeoffs in both runtimes and the browser.
2. Credential safety: `secure-gist-sync-custody` — remove persistent token custody before touching cloud-save presentation.
3. Observability contract: `lifecycle-source-coherence-guard` — make local launch truth executable, then send authoritative drift through Ark.
4. Refresh the Genius List to prove the primary queue is exhausted.
5. Generate the Innovation Pack, implement second-order candidates, and run compound refinements across the three shipped surfaces.
6. Verify focused behavior after each item, then run direct canonical shards, browser UI, Pages staging, security, canon, release, and doctor gates.

Every audit item uses its L2 recipe or higher. Gameplay changes must be browser-visible and focused-test-covered; source-of-truth claims require direct evidence.
